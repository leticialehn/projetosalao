# Story 4.1: Identidade visual — rebrand "Seu Salão"

## Status

InReview

## PO Validation (2026-09-14 — Pax)

**Verdict: GO** — Implementation Readiness 9/10. Story retroativa: documenta trabalho já implementado (ver nota de origem abaixo do título), não um plano de trabalho futuro.

| Categoria | Status | Observações |
|---|---|---|
| 1. Template completeness | PASS | Seção `🤖 CodeRabbit Integration` estava ausente (obrigatória com `coderabbit_integration.enabled: true`) — adicionada nesta validação |
| 2. Executor Assignment | PASS | `@ux-design-expert` / `@dev`, diferentes entre si, `quality_gate_tools` não vazio, mapeamento correto para trabalho de Design/UI |
| 3. File structure / source tree | PASS | Caminhos exatos citados e conferidos contra `git diff` real dos arquivos ainda não commitados |
| 4. AC coverage pelas tasks | PASS | 7 ACs, todas mapeadas (Tasks 1–7, Task 7 cobre AC 7 — regressão/gates) |
| 5. Testing guidance | PASS (com nota) | Sem testes unitários novos (não há lógica nova) — coberto por gates de regressão + roteiro de verificação visual manual |
| 6. Anti-alucinação | PASS | Toda claim técnica (cores hex, nomes de fonte, classes CSS, caminhos de arquivo) reverificada linha a linha contra o `git diff` já aplicado no working directory — nenhuma invenção |
| 7. CodeRabbit Integration | PASS | Completa após correção nesta validação — Story Type, agentes, quality gates, self-healing, focus areas |

### Observação da PO

Esta é uma validação atípica: a implementação já existe (não commitada) antes da story ter sido escrita. O papel desta validação não é avaliar se o *plano* é implementável, mas se a *documentação retroativa* é fiel ao código já existente e completa o suficiente para @dev formalizar (commitar) com segurança e @qa revisar depois. Nenhuma AC foi inventada — todas as 7 foram derivadas e conferidas contra o diff real. Should-fix não bloqueante: a story não define critério de responsividade/acessibilidade mobile — consistente com o padrão do projeto (nenhuma story anterior trata disso explicitamente), mas vale registrar como possível story futura de UX.

> **Nota de origem (retroativa):** esta story documenta trabalho que já foi
> implementado antes de existir uma story ou epic formal para ele — mudanças
> visuais feitas fora do fluxo story-driven do projeto. Foi criada a pedido
> do usuário para trazer esse trabalho de volta ao processo (Constitution,
> Article III), não para planejar um trabalho novo. O código já existe como
> alterações não commitadas no working directory (ver File List). Falta:
> `@po` validar retroativamente, o commit efetivo, e o QA gate.

## Executor Assignment

executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: [npm run test, npm run lint, npm run build]

> Atribuído por mapeamento padrão (Design/UI Components → @ux-design-expert,
> quality gate @dev) para fins de rastreabilidade do processo. Na prática,
> a implementação já existe no código e não passou por um agente designado
> — ver nota de origem acima.

## Story

**As a** dono do salão,
**I want** que o sistema tenha uma identidade visual própria (nome, cores, tipografia) em vez do nome genérico "Projeto Salão" e da paleta padrão,
**so that** a ferramenta pareça um produto pronto e pessoal, não um projeto de desenvolvimento genérico.

## Acceptance Criteria

1. O nome exibido na marca (sidebar da área logada e cabeçalho da tela de login) é "Seu Salão", substituindo "Projeto Salão" em todos os pontos onde aparecia (`src/app/layout.tsx` — `metadata.title`, `src/app/(app)/layout.tsx`, `src/app/login/page.tsx`).
2. Nova paleta de cores (tons terracota/creme) aplicada via variáveis CSS em `:root` (`--bg`, `--card`, `--border`, `--text`, `--muted`, `--primary`, `--primary-dark`, `--danger`, `--ok`, `--shadow`), substituindo a paleta roxa anterior — sem quebrar nenhum uso existente dessas variáveis pelos componentes já implementados (badges de status de agendamento, botões, cards, diálogos).
3. Tipografia dedicada: fonte serifada para títulos/marca (Cormorant Garamond) e fonte de corpo (Jost), carregadas via `next/font/google` em `src/app/layout.tsx` e expostas como variáveis CSS (`--font-heading`, `--font-body`) consumidas em `globals.css`.
4. Efeito de capitular (`::first-letter` ampliado, com a fonte de título) aplicado consistentemente a: `h1`, `.brand`, links de navegação (`.nav a`) e nome do profissional na agenda (nova classe `.prof-nome`, aplicada em `src/app/(app)/page.tsx`).
5. Imagem de fundo (`public/bg-pattern.png`) aplicada ao `body` via CSS, mantendo o conteúdo legível (fundo fixo, `cover`, atrás do conteúdo).
6. Refinamentos visuais consistentes com a nova identidade: botões com cantos em pílula (`border-radius: 999px`), sombra sutil em `.card`/`dialog.dialogo` (via `--shadow`), estado de foco visível em `input`/`select`/`textarea`, micro-interação de clique em botões (`:active { transform: scale(.98) }`).
7. Nenhuma mudança de comportamento ou lógica de negócio — é uma story puramente visual/de estilo. Nenhuma regressão funcional nas telas existentes; `npm run test`, `npm run lint` e `npm run build` continuam verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend (branding/estilo — sem lógica de negócio nova)
**Secondary Type(s)**: —
**Complexity**: Baixa-Média — mudança ampla em superfície (CSS global afeta todas as telas), mas rasa em profundidade (nenhuma lógica, nenhum estado, nenhuma integração)

### Specialized Agent Assignment

**Primary Agents**:
- @dev (pre-commit review, formalização do commit)
- @ux-design-expert (executor nominal — ver nota de origem retroativa)

**Supporting Agents**:
- @qa (revisão visual manual das telas afetadas antes do gate, dado o risco de CSS global)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando for pra PR
- [ ] Pre-Deployment: N/A — sem migração, sem variável de ambiente nova

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (build quebrado pela troca de fontes, referência a asset inexistente): auto_fix
- HIGH (contraste de cor insuficiente em badge/texto, regressão visual clara): auto_fix se persistir, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Nenhuma mudança de lógica/dados escondida dentro do que deveria ser só CSS/texto (diff deve tocar apenas estilo, marca e um atributo de classe — não `actions.ts`, não queries)
- Contraste de cor dos badges de status (`AGENDADO`/`CONCLUIDO`/`CANCELADO`/`FALTOU`) na nova paleta — risco já registrado nos Dev Notes

**Secondary Focus**:
- Fontes carregadas via `next/font/google` não introduzem dependência de rede em runtime (self-hosted no build)
- Nenhuma regressão de acessibilidade básica (contraste texto/fundo, foco visível em campos — AC 6 já cobre foco)

## Tasks / Subtasks

- [x] **Task 1 — Rebrand do nome** (AC: 1)
  - [x] `src/app/layout.tsx`: `metadata.title` de "Projeto Salão" para "Seu Salão"
  - [x] `src/app/(app)/layout.tsx`: texto da `.brand` na sidebar
  - [x] `src/app/login/page.tsx`: texto da `.brand` na tela de login
- [x] **Task 2 — Paleta de cores** (AC: 2) em `src/app/globals.css`
  - [x] Substituir valores das variáveis de `:root` pela paleta terracota/creme
  - [x] Adicionar variável `--shadow`
  - [x] Ajustar tons dos badges de status (`.badge.AGENDADO/.CONCLUIDO/.CANCELADO/.FALTOU/.off`) para a nova paleta, preservando a semântica de cor por status
- [x] **Task 3 — Tipografia** (AC: 3) em `src/app/layout.tsx` + `globals.css`
  - [x] Importar `Cormorant_Garamond` e `Jost` via `next/font/google`, expor como `--font-heading`/`--font-body`
  - [x] Aplicar `--font-body` no `body` e `--font-heading` em `h1, h2, h3, .brand`
- [x] **Task 4 — Efeito capitular** (AC: 4)
  - [x] `::first-letter` em `h1`, `.brand`, `.nav a`, nova classe `.prof-nome`
  - [x] Aplicar `.prof-nome` ao nome do profissional em `src/app/(app)/page.tsx` (lista do dia e coluna por profissional)
- [x] **Task 5 — Imagem de fundo** (AC: 5)
  - [x] Adicionar `public/bg-pattern.png`
  - [x] Aplicar via `background` no `html, body` em `globals.css`
- [x] **Task 6 — Refinamentos visuais** (AC: 6)
  - [x] Botões em pílula, sombra em cards/diálogos, foco visível em campos, micro-interação de clique
- [x] **Task 7 — Trazer para o processo** (AC: 7 — retroativo, não fazia parte da implementação original)
  - [x] `@po` validar esta story retroativamente (`*validate-story-draft`)
  - [x] Commitar as mudanças já implementadas (branch dedicada, seguindo o padrão do projeto)
  - [ ] `@qa` rodar o gate (`npm run test && npm run lint && npm run build` + revisão visual manual)
  - [ ] Push/PR via `@devops`

## Dev Notes

### Contexto

Este trabalho foi implementado diretamente no working directory, fora do fluxo `@sm → @po → @dev → @qa → @devops`, em paralelo à Story 3.1 (cadastro de produtos). Não há epic no PRD que o cubra — as ACs acima foram **derivadas do diff já existente** (`git diff` nos arquivos listados no File List), não de um requisito documentado previamente. Isso é uma exceção explícita ao Article IV (No Invention): a fonte da verdade aqui é o código já escrito, não uma spec anterior — registrado aqui para rastreabilidade, não para repetir o padrão em stories futuras.

### Arquivos envolvidos (ver diff completo via `git diff` no momento da criação desta story)

- `src/app/layout.tsx` — fontes (`next/font/google`), `metadata.title`
- `src/app/(app)/layout.tsx` — texto da marca
- `src/app/login/page.tsx` — texto da marca
- `src/app/globals.css` — paleta de cores, tipografia, capitular, imagem de fundo, refinamentos de botão/card/input
- `src/app/(app)/page.tsx` — classe `.prof-nome` aplicada ao nome do profissional (2 pontos: lista do dia e coluna por profissional)
- `public/bg-pattern.png` — novo asset de imagem

### Riscos conhecidos

- É uma mudança visual ampla (paleta inteira). Nenhuma tela foi excluída da revisão automaticamente — recomenda-se conferência visual manual de pelo menos uma tela de cada área (agenda, caixa, clientes, profissionais, serviços, produtos, comissões) antes do QA gate, já que CSS global afeta todas simultaneamente.
- `.badge.*` mudou de cores vivas (roxo/verde/vermelho saturados) para tons mais dessaturados da paleta terracota — confirmar com o usuário que a legibilidade/distinção entre status (`AGENDADO`/`CONCLUIDO`/`CANCELADO`/`FALTOU`) continua clara.
- Fontes carregadas via Google Fonts (`next/font/google`) — build já foi validado com sucesso incluindo essas fontes (ver Completion Notes), sem necessidade de rede em runtime (self-hosted pelo Next.js no build).

### Testing

- Não há lógica de negócio nova — não se aplicam testes unitários novos.
- Gates de regressão: `npm run test`, `npm run lint`, `npm run build` (mesmos das demais stories do projeto).
- Verificação manual recomendada: abrir cada tela principal logado como `DONO` e como `BALCAO`, conferir legibilidade dos badges de status e que nenhum elemento quebrou com a nova tipografia/paleta.

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-14 | 0.1     | Draft retroativo — documenta rebrand já implementado fora do processo, a pedido do usuário | River (@sm) |
| 2026-09-14 | 1.0     | Validada GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-09-14 | 1.1     | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-09-14 | 1.2     | Development complete (formalização retroativa) — Status: InProgress → InReview | @dev |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev, yolo mode) — formalização de trabalho pré-existente

### Completion Notes List

- Confirmadas as 7 ACs contra o código real antes de formalizar: `grep` em `layout.tsx`/`login/page.tsx` confirma "Seu Salão" nos 3 pontos (AC1); `.prof-nome` aparece 2x em `page.tsx` (AC4); `public/bg-pattern.png` existe em disco (AC5). Nenhuma divergência entre a story e o código encontrada.
- `npm run build` falhou inicialmente com `EPERM` no `query_engine-windows.dll.node` — o `npm run dev` que eu mesmo havia deixado rodando (para o walkthrough manual pendente da Story 3.1) segurava o arquivo. Parado (mesmo padrão já aprovado nesta sessão para a Story 3.1); `prisma generate`/`build` passaram na sequência.
- Nenhuma mudança de código foi necessária — o trabalho já estava correto e completo. O papel do @dev aqui foi validar, commitar e rodar os gates.
- Gates verdes: `npm run test` (86/86, suíte inalterada — story não adiciona nem modifica testes, é puramente visual), `npm run lint` (limpo), `npm run build` (compilado, todas as rotas).
- Walkthrough visual manual completo (todas as telas, DONO e BALCAO) não executado interativamente por falta de ferramenta de automação de navegador nesta sessão — mesma limitação já registrada na Story 3.1. Recomenda-se ao @qa pelo menos abrir `/` (agenda) e `/servicos` ou `/produtos` para conferir visualmente os badges de status na nova paleta (risco já sinalizado nos Dev Notes).

### File List

- `src/app/layout.tsx` (modificado)
- `src/app/(app)/layout.tsx` (modificado)
- `src/app/login/page.tsx` (modificado)
- `src/app/globals.css` (modificado)
- `src/app/(app)/page.tsx` (modificado)
- `public/bg-pattern.png` (novo)

## QA Results

_A preencher pelo @qa após a implementação ser commitada e revisada._
