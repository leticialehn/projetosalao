# Story 2.4: Comissão por serviço

## Status

InReview

## PO Validation (2026-09-11 — Pax)

**Verdict: GO** — Clarity 9/10. Depende de 1.3/1.8/2.3 (Done). Migração aditiva (unique composta).

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Valor claro; fora de escopo o toggle bruto/líquido (Story 2.5) |
| 2. Technical Implementation Guidance | PASS | Delta de schema, assinaturas das funções puras, arquivos nomeados |
| 3. Reference Effectiveness | PASS | Todo Dev Note cita o código atual real (schema.prisma, financeiro.ts, actions.ts, ProfissionalRow.tsx, comissoes/page.tsx) |
| 4. Self-Containment | PASS | Quebra de cardinalidade 1:1→1:N nos 3 arquivos consumidores e o cuidado com NULL único no SQLite ficam explícitos |
| 5. Testing Guidance | PASS | Casos da função pura + roteiro manual com números (R$100 a 60% vs 40%) |
| 6. CodeRabbit Integration | PASS | Tipo Database+API+Frontend, agentes, gates, self-healing completos |

### Decisões do PO (confirmando o Dev Notes)

1. **Manter `comissaoPorProfissional` ao lado da nova função ou substituí-la** fica a critério do @dev — se `/comissoes` for o único consumidor confirmado (verificar com Grep antes), pode migrar direto; senão, manter as duas. **Não bloqueante.**
2. **Cuidado da unicidade (SQLite permite múltiplos `NULL`)** — aceito como documentado; suficiente para o escopo (instância única, sem escrita concorrente). **Aprovado.**

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** definir uma comissão diferente para um serviço específico de um profissional,
**so that** serviços com custo de produto ou margem diferente sejam remunerados de forma justa.

## Acceptance Criteria

1. `ComissaoRegra` passa a aceitar escopo por serviço: além da regra geral (`servicoId` nulo), é possível ter regras com `servicoId` preenchido para o mesmo profissional. A unicidade muda de `profissionalId @unique` para `@@unique([profissionalId, servicoId])`. A migração é aditiva — as linhas existentes (uma por profissional, hoje `profissionalId @unique`) continuam válidas como a regra geral (`servicoId = null`); nenhum dado é perdido ou recriado.
2. `src/lib/financeiro.ts` ganha função **pura** que resolve o percentual de um atendimento: regra do serviço se existir para aquele profissional, senão a regra geral do profissional, senão `0`. Coberta por testes (só geral; geral + serviço específico; nem geral nem serviço → 0; serviço específico sem geral).
3. A tela de edição de profissional (`/profissionais`) lista os serviços ativos e permite definir um `%` por serviço, além do `%` geral já existente (Story 1.3). Campo vazio = sem regra específica para aquele serviço (usa a geral).
4. O relatório de comissões (`/comissoes`) detalha, por profissional, a quebra por serviço **quando houver regra específica** para pelo menos um serviço daquele profissional; o total por profissional continua correto (igual à soma das comissões calculadas por atendimento, não a uma média).
5. **Sem regra específica**, o comportamento é idêntico ao Epic 1: comissão = `% geral × total cobrado`. Regressão coberta por teste (todos os testes existentes de `comissaoPorProfissional` continuam passando).
6. `npm run test`, `npm run lint`, `npm run build` verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Database (unique constraint muda de simples para composta, cardinalidade `Profissional.comissaoRegra` vira 1:N) + API (nova função pura de resolução de percentual) + Frontend (edição por serviço + relatório com quebra)
**Secondary Type(s)**: —
**Complexity**: Média-Alta (a mudança de cardinalidade da relação `comissaoRegra` afeta 3 arquivos existentes que hoje assumem 1:1)

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco na mudança de cardinalidade e em `SEM regra específica = comportamento idêntico ao Epic 1`)

**Supporting Agents**:
- @data-engineer (referência: unique composta com coluna nula — ver "Cuidado" em Dev Notes)

### Quality Gate Tasks

- [x] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando o Epic 2 for para PR
- [ ] Pre-Deployment: `prisma migrate deploy` já roda no start (Railway); nenhuma ação extra de seed é necessária (regras existentes viram regra geral automaticamente)

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (migração destrutiva, regressão em `comissaoPorProfissional` sem regra específica, total do profissional errado): auto_fix
- HIGH (duplicidade de regra geral por profissional, quebra de `/comissoes` ou `/profissionais` após mudar a relação para lista): auto_fix se persistir na 2ª iteração, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- `comissaoPorProfissional` (Epic 1) **não muda de comportamento** quando não há regra por serviço — validar com os testes existentes
- Toda leitura de `profissional.comissaoRegra` (singular) precisa virar `profissional.comissaoRegras` (lista) nos 3 arquivos afetados (ver Dev Notes)
- Migração 100% aditiva (nenhuma coluna removida, nenhuma linha apagada)

**Secondary Focus**:
- Unicidade da regra geral por profissional garantida pela aplicação, não só pelo banco (ver "Cuidado" em Dev Notes — SQLite permite múltiplos `NULL` num índice único)
- `/profissionais` protegida no servidor (padrão já usado desde a 1.3/2.2 — `exigirSessao`/`exigirPapel`)
- Relatório `/comissoes` não quebra quando um profissional não tem nenhuma regra (nem geral, nem por serviço) — comissão 0, sem erro

## Tasks / Subtasks

- [x] **Task 1 — Schema** (AC: 1) em `prisma/schema.prisma`
  - [x] `ComissaoRegra`: `+ servico Servico? @relation(fields: [servicoId], references: [id])`, `+ servicoId String?`
  - [x] `ComissaoRegra.profissionalId`: remover `@unique` da coluna; adicionar `@@unique([profissionalId, servicoId])`
  - [x] `Profissional.comissaoRegra ComissaoRegra?` → `comissaoRegras ComissaoRegra[]`
  - [x] `Servico`: adicionar back-relation `comissaoRegras ComissaoRegra[]`
  - [x] `npx prisma migrate dev --name epic2_comissao_por_servico` — conferir: SQLite deve gerar table-rebuild de `ComissaoRegra` (como aconteceu com `FechamentoCaixa` na Story 2.3); as linhas existentes devem preservar `profissionalId` com `servicoId` nulo (regra geral)
- [x] **Task 2 — Função pura de resolução** (AC: 2) em `src/lib/financeiro.ts`
  - [x] `export interface RegraComissao { servicoId: string | null; percentual: number }`
  - [x] `export function percentualComissao(regras: RegraComissao[], servicoId: string): number` — procura regra com `servicoId` igual; se não achar, procura a regra com `servicoId === null` (geral); se nenhuma, retorna `0`
  - [x] `AtendimentoInput` ganha `servicoId: string`
  - [x] Nova função `comissaoPorProfissionalComServico(atendimentos: AtendimentoInput[], regrasPorProfissional: Record<string, RegraComissao[]>)` — mesmo agregado de `comissaoPorProfissional` (qtd, totalCobrado, comissao por profissional, usando `percentualComissao` por atendimento) **mais** um agregado por `(profissionalId, servicoId)` para alimentar a quebra do relatório
  - [x] **Não** alterar a assinatura nem o comportamento de `comissaoPorProfissional` existente (mantida para não quebrar os testes/consumidores atuais até a página de comissões migrar — ou substituí-la e migrar `/comissoes` no mesmo Task; decisão do @dev, documentar no Dev Notes/Change Log qual caminho foi seguido)
- [x] **Task 3 — Testes** (AC: 2, 5) em `src/lib/financeiro.test.ts`
  - [x] `percentualComissao`: só geral; geral + específico do serviço (específico prevalece); nem geral nem específico → 0; específico de outro serviço não interfere
  - [x] `comissaoPorProfissionalComServico`: mistura de atendimentos com e sem regra específica; total por profissional bate com a soma manual; regressão — sem nenhuma regra por serviço, o resultado é idêntico ao de `comissaoPorProfissional` com o mesmo `percentualPorProfissional`
- [x] **Task 4 — Edição por serviço** (AC: 3) em `src/app/(app)/profissionais/`
  - [x] `page.tsx`: buscar `Servico` ativos (`prisma.servico.findMany({ where: { ativo: true } })`) e passar para `ProfissionalRow`; trocar `include: { comissaoRegra: true }` por `include: { comissaoRegras: true }`
  - [x] `ProfissionalRow.tsx`: prop `comissaoRegras: { servicoId: string | null; percentual: number }[]` no lugar de `comissaoRegra`; percentual geral = `comissaoRegras.find(r => r.servicoId === null)?.percentual ?? comissaoPercentual ?? 0` (mantém o fallback já existente da Story 1.3); lista os serviços ativos com um campo de `%` (vazio = sem regra específica) preenchido com `comissaoRegras.find(r => r.servicoId === servico.id)?.percentual`
  - [x] `actions.ts`: nova action `definirComissaoServico(_prev, formData)` — recebe `profissionalId`, `servicoId`, `percentual` (vazio = remover a regra específica, se existir); mesma validação 0..100 e guarda `PAPEL_DONO` de `definirComissao`; `upsert`/`delete` por `profissionalId_servicoId` (nome do índice composto gerado pelo Prisma para `@@unique([profissionalId, servicoId])`)
  - [x] `definirComissao` (regra geral existente): ajustar o `upsert` para usar a chave composta `profissionalId_servicoId: { profissionalId, servicoId: null }` em vez de `profissionalId`
- [x] **Task 5 — Relatório com quebra por serviço** (AC: 4) em `src/app/(app)/comissoes/page.tsx`
  - [x] Trocar `include: { comissaoRegra: true }` por `include: { comissaoRegras: true }`; buscar `Servico` (para nomes) e incluir `servicoId` na query de atendimentos (hoje só busca `profissionalId`, `valorCobrado`)
  - [x] Montar `regrasPorProfissional: Record<string, RegraComissao[]>` a partir de `comissaoRegras`
  - [x] Usar `comissaoPorProfissionalComServico`; exibir a quebra por serviço (nome do serviço, qtd, total, comissão) só para profissionais que tenham **pelo menos uma** regra com `servicoId` não nulo — os demais continuam mostrando só a linha do profissional, sem quebra (comportamento do Epic 1 preservado)
- [x] **Task 6 — Validar**: `npm run test && npm run lint && npm run build` + teste manual

## Dev Notes

### Contexto (Stories 1.3, 1.8, 2.3)

- `src/lib/financeiro.ts` hoje: `comissao(valorCobrado, percentual)`, `comissaoPorProfissional(atendimentos, percentualPorProfissional)`. `AtendimentoInput` tem só `{ valorCobrado, profissionalId }` — **não** tem `servicoId` ainda. `[Source: src/lib/financeiro.ts]`
- `prisma/schema.prisma` hoje: `ComissaoRegra { id, profissional Profissional @relation(...), profissionalId String @unique, percentual Float, criadoEm }`; `Profissional.comissaoRegra ComissaoRegra?` (1:1). `Agendamento` já tem `servicoId String` (obrigatório, relação com `Servico`) — não precisa de migração no `Agendamento`. `[Source: prisma/schema.prisma]`
- `src/app/(app)/profissionais/actions.ts`: `definirComissao` faz `prisma.$transaction([comissaoRegra.upsert({ where: { profissionalId }, ... }), profissional.update({ data: { comissaoPercentual } })])` — o campo `Profissional.comissaoPercentual` é um **espelho legado** da Story 1.3; `/comissoes` já trata `ComissaoRegra` como fonte canônica (`comissaoRegra?.percentual ?? 0`), então o espelho pode ficar desatualizado sem quebrar nada, mas manter a escrita por consistência com o dado existente. `[Source: src/app/(app)/profissionais/actions.ts]`
- `src/app/(app)/profissionais/ProfissionalRow.tsx`: hoje usa `p.comissaoRegra?.percentual ?? p.comissaoPercentual ?? 0` (singular). `[Source: src/app/(app)/profissionais/ProfissionalRow.tsx]`
- `src/app/(app)/comissoes/page.tsx`: busca `prisma.profissional.findMany({ include: { comissaoRegra: true } })` e monta `percentualPorProfissional[p.id] = p.comissaoRegra?.percentual ?? 0`; a query de atendimentos só seleciona `profissionalId`, `valorCobrado` (precisa passar a selecionar `servicoId` também). `[Source: src/app/(app)/comissoes/page.tsx]`

### Cuidado — unicidade da regra geral (@data-engineer)

SQLite trata cada `NULL` como distinto num índice único, então `@@unique([profissionalId, servicoId])` **não impede**, no nível do banco, duas linhas com o mesmo `profissionalId` e `servicoId = NULL` (duas "regras gerais" pro mesmo profissional). O Prisma resolve a busca (`findUnique`/`upsert` pela chave composta) corretamente com `servicoId: null` porque traduz para `IS NULL` na query — então, **desde que toda escrita passe pelas actions desta story** (`definirComissao` / `definirComissaoServico`, ambas usando a chave composta), a unicidade fica garantida na prática. Não é necessário um `CHECK` ou índice parcial para o escopo desta story; documentar a suposição no Change Log.

### Decisões a tomar (registrar no Change Log)

1. **Substituir `comissaoPorProfissional` ou adicionar `comissaoPorProfissionalComServico` ao lado dela?** Recomendação: adicionar a nova função e migrar `/comissoes` para usá-la nesta story (a antiga fica só nos testes de regressão/Story 1.8, se ainda fizer sentido mantê-la, ou é removida se `/comissoes` era o único consumidor — conferir com `Grep` antes de remover).
2. **Layout da quebra por serviço no relatório:** recomendação — abaixo da linha do profissional (linha "pai"), uma sub-lista/sub-tabela com os serviços que têm regra específica (nome do serviço, qtd, total, comissão). Profissionais sem nenhuma regra específica não ganham essa sub-lista. Ajuste fino de layout é decisão do @dev; sem necessidade de @ux-design-expert para esta story.

### Assunções explícitas

- Regra por serviço é **por profissional** (o mesmo serviço pode ter % diferente para profissionais diferentes) — já é assim que `ComissaoRegra` se relaciona (`profissionalId` na regra).
- A comissão continua calculada sobre o **valor cobrado bruto** nesta story; o toggle bruto/líquido é a Story 2.5 (não implementar aqui).
- Serviço inativo mantém a regra específica já criada (só não aparece na lista de edição para criar uma nova); não é necessário migrar/limpar regras de serviços desativados.

### Edge cases

- Profissional sem nenhuma `ComissaoRegra` (nem geral, nem por serviço) → comissão 0 em todos os atendimentos, sem erro (comportamento já coberto pela Story 1.3/1.8).
- Profissional com regra geral mas sem nenhuma regra por serviço → comportamento idêntico ao Epic 1 (AC 5); sem quebra no relatório.
- Atendimento de um serviço que foi excluído/desativado depois de concluído → `servicoId` no `Agendamento` continua apontando pro serviço (nunca é apagado, só desativado — Story de exclusão de serviço já trata isso); a resolução do percentual funciona normalmente.
- `percentual` fora de 0..100 na regra por serviço → mesmo erro pt-BR da regra geral, não grava.

### Testing

- **Unit:** ver Task 3.
- **Manual:**
  1. Definir comissão geral de um profissional em 40%.
  2. Definir comissão específica de 60% para um serviço desse profissional.
  3. Concluir um atendimento desse serviço com esse profissional, valor R$ 100 → `/comissoes` no período mostra R$ 60,00 pra esse atendimento (não R$ 40,00).
  4. Concluir um atendimento de **outro** serviço do mesmo profissional, valor R$ 100 → conta com os 40% gerais (R$ 40,00).
  5. `/comissoes`: total do profissional = R$ 100,00 (60 + 40); a quebra por serviço aparece só para o serviço com regra específica.
  6. Profissional sem nenhuma regra por serviço: relatório idêntico ao de antes da story (sem quebra, só a linha do profissional).
  7. `/profissionais` como balcão → redireciona (regra da Story 2.2, sem mudança aqui).

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-11 | 0.1     | Draft inicial da story | River (@sm)  |
| 2026-09-11 | 1.0     | Validada GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-09-11 | 1.1     | Desenvolvimento iniciado (yolo) — Status: Ready → InProgress | Dex (@dev) |
| 2026-09-11 | 1.2     | Implementada — Ready → InProgress → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex)

### Completion Notes List

- **Schema:** `ComissaoRegra` ganhou `servicoId String?` (FK opcional pra `Servico`) e a unique passou de `profissionalId @unique` para `@@unique([profissionalId, servicoId])`. Migração `epic2_comissao_por_servico`: table-rebuild do SQLite (como previsto), preservando as linhas existentes via `INSERT...SELECT` — `servicoId` veio `NULL` para todas (viraram a regra geral automaticamente). `Profissional.comissaoRegra` (1:1) → `comissaoRegras` (1:N); `Servico` ganhou a back-relation `comissaoRegras`.
- **Decisão (Task 2, item 6 do Dev Notes):** removida `comissaoPorProfissional` em vez de mantê-la ao lado da nova — `/comissoes` era o único consumidor fora dos testes (confirmado por Grep), então migrar direto evitou código morto. Substituída por `percentualComissao` (resolução do percentual: serviço específico > geral > 0) e `comissaoPorProfissionalComServico` (agregado por profissional **e** por serviço).
- **Desvio do Dev Notes — unicidade da regra geral:** o "Cuidado" do Dev Notes assumia que `upsert` com `where: { profissionalId_servicoId: { profissionalId, servicoId: null } }` funcionaria (Prisma traduzindo pra `IS NULL`). **Não funciona** — o Prisma client não aceita `null` no tipo do `where` de uma unique composta (`ComissaoRegraProfissionalIdServicoIdCompoundUniqueInput` exige `servicoId: string`, erro de build). Corrigido em `definirComissao`: troca de `upsert` por `findFirst` (`profissionalId` + `servicoId: null`) seguido de `update`/`create` dentro de uma transação interativa (`$transaction(async (tx) => ...)`). `definirComissaoServico` (regra por serviço) não teve esse problema — `servicoId` ali é sempre uma string real, então o `upsert` pela chave composta funciona normalmente.
- **Testes:** os 9 testes antigos de `comissaoPorProfissional` viraram testes de regressão de `comissaoPorProfissionalComServico` (mesmos cenários, com `servicoId` + regra geral — provam que sem regra por serviço o resultado é idêntico ao Epic 1). Mais 4 testes de `percentualComissao` (só geral, específica prevalece, nem uma nem outra, específica de outro serviço não interfere) e 3 novos de `comissaoPorProfissionalComServico` (mistura com/sem regra específica, `porServico` correto, independência entre serviços). **72 no total** (era 69 antes; -9 dos antigos +12 novos).
- **`/profissionais`:** lista os serviços ativos por profissional em edição; um campo de `%` por serviço (vazio = usa a geral) via nova action `definirComissaoServico` (upsert por `profissionalId_servicoId`, ou `deleteMany` quando o campo é salvo vazio — remove a regra específica). Resumo da linha (modo leitura) ganhou "(N por serviço)" quando há regras específicas.
- **`/comissoes`:** query de atendimentos passou a selecionar `servicoId`; quebra por serviço (nome, qtd, total, comissão) só renderiza para profissionais com pelo menos uma regra específica — os demais continuam idênticos ao Epic 1 (`porServico: []`).
- **Seed:** exemplo de regra específica adicionado (Bruno: 35% geral, 50% em Coloração) pra já nascer com um caso de teste manual pronto.
- **Verificação** (browser não alcança localhost, mesma limitação registrada na Story 2.3 — confirmado nesta sessão: `claude-in-chrome` não consegue navegar até `http://localhost:3000`): scripts Node diretos contra o Prisma client, simulando exatamente a lógica de `page.tsx`.
  - Regras seedadas: Bruno `[{servico: null, pct: 35}, {servico: <coloracao>, pct: 50}]` — confirmado que a regra geral e a específica coexistem (a preocupação do "Cuidado" sobre múltiplos `NULL` não se aplica aqui; só uma regra geral por profissional é criada, sempre pela action).
  - Atendimento real criado (Bruno, Coloração, R$200, CONCLUIDO): agregação resultou em comissão **R$100,00** (200×50%), não R$70,00 (200×35%) — confirma que a regra específica prevalece (AC 2, 4).
  - Ana (sem regra por serviço), atendimento seedado R$80 em Corte a 40%: comissão R$32,00 — idêntico ao que o Epic 1 calcularia (AC 5, sem regressão).
  - Todas as 7 rotas protegidas (`/agendamentos`, `/caixa`, `/clientes`, `/painel`, `/servicos`, `/taxas`, `/usuarios`) seguem redirecionando (307) sem sessão — sem regressão no guard de rotas.
  - Banco resetado ao estado limpo do seed (`npm run db:seed -- --force`) após a verificação.
- **CodeRabbit:** não executado — WSL sem distribuição instalada neste ambiente (`wsl.exe` sem distros). Graceful degradation conforme `core-config.yaml` (`skip_if_not_installed: true`); revisão manual aplicada no lugar.
- Gates: `npm run test` 72/72, `npm run lint` sem warnings, `npm run build` verde (Next.js 15.5.25, typecheck incluso).

### File List

**Novos:** `prisma/migrations/20260911112256_epic2_comissao_por_servico/`

**Modificados:** `prisma/schema.prisma`, `prisma/seed.ts`, `src/lib/financeiro.ts`, `src/lib/financeiro.test.ts`, `src/app/(app)/profissionais/actions.ts`, `src/app/(app)/profissionais/page.tsx`, `src/app/(app)/profissionais/ProfissionalRow.tsx`, `src/app/(app)/comissoes/page.tsx`
