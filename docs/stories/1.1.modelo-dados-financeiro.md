# Story 1.1: Modelo de dados financeiro e de comissão

## Status

Done

## PO Validation (2026-09-09 — Pax)

**Verdict: GO** — Clarity 9/10. Ready for implementation.

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Objetivo, valor e dependências (nenhuma anterior; desbloqueia 1.2+) claros |
| 2. Technical Implementation Guidance | PASS | DDL alvo, arquivos, `onDelete`, índices e ordem do seed especificados |
| 3. Reference Effectiveness | PASS | Aponta `docs/architecture.md#2`; info crítica resumida na própria story |
| 4. Self-Containment | PASS | Decisões (String status, migrate dev, espelho de comissão) explícitas |
| 5. Testing Guidance | PASS | Verificação manual em 4 passos; sem teste automatizado é adequado aqui |
| 6. CodeRabbit Integration | PASS | Seção completa, tipo Database, self-healing light coerente |

Não bloqueante (para o dev observar): confirmar que `prisma migrate dev` roda sem servidor de banco (SQLite é arquivo — ok) e que o `.gitignore` versiona `prisma/migrations/` mas ignora `prisma/dev.db`.

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [prisma validate, prisma migrate diff, npm run build]

## Story

**As a** desenvolvedor,
**I want** as novas entidades e campos no schema Prisma com migração não destrutiva,
**so that** as demais stories tenham base para gravar pagamentos, comissões e fechamentos.

## Acceptance Criteria

1. `schema.prisma` inclui `Pagamento` (id, agendamentoId, valor, formaPagamento, dataHora, conferido default false, criadoEm) com `onDelete: Cascade` na relação com `Agendamento`.
2. `schema.prisma` inclui `ComissaoRegra` (id, profissionalId `@unique`, percentual, criadoEm) com `onDelete: Cascade`, e `FechamentoCaixa` (id, data `@unique`, totalDinheiro, totalPix, totalDebito, totalCredito, totalGeral, qtdAtendimentos, ticketMedio, observacoes?, criadoEm).
3. `Agendamento` ganha `valorCobrado Float?`; o comentário de status passa a listar `AGENDADO | CONCLUIDO | CANCELADO | FALTOU`.
4. `Profissional` ganha `comissaoPercentual Float @default(0)` e relação opcional `comissaoRegra`.
5. A migração aplica sobre um banco com dados existentes sem apagar agendamentos, serviços, profissionais ou clientes (todos os campos novos são `?` ou `@default`).
6. `npm run db:seed` popula ao menos 2 `ComissaoRegra` e alguns `Pagamento` de exemplo consistentes com agendamentos `CONCLUIDO` (com `valorCobrado` preenchido).
7. `prisma/migrations/` passa a existir com a migração `epic1_financeiro`; `package.json` ganha `"db:migrate": "prisma migrate dev"`.
8. `npm run build` (roda `prisma generate` + `next build`) e o typecheck passam.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Database (schema, migração, seed)
**Secondary Type(s)**: —
**Complexity**: Baixa

### Specialized Agent Assignment

**Primary Agents**:
- @data-engineer (executor — DDL, migração, seed)
- @dev (quality gate — valida generate/build)

**Supporting Agents**:
- @architect (referência: `docs/architecture.md` §2)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `prisma validate`, `prisma migrate diff`, `npm run build` antes de marcar concluída
- [ ] Pre-PR (@devops): quando o epic for para PR
- [ ] Pre-Deployment: N/A (sem produção)

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (2 iter)
- HIGH issues: document_as_debt se persistir

### CodeRabbit Focus Areas

**Primary Focus**:
- Migração não destrutiva (nenhum `DROP`/`NOT NULL` sobre coluna existente sem default)
- Integridade referencial: `onDelete` explícito nas novas relações

**Secondary Focus**:
- Seed idempotente e consistente (soma de pagamentos == `valorCobrado`)
- Índices declarados nos campos consultados (`Pagamento.dataHora`, `Pagamento.agendamentoId`)

## Tasks / Subtasks

- [ ] **Task 1 — Estender `prisma/schema.prisma`** (AC: 1, 2, 3, 4)
  - [ ] Adicionar model `Pagamento` com `@@index([dataHora])` e `@@index([agendamentoId])` e relação `agendamento Agendamento @relation(fields: [agendamentoId], references: [id], onDelete: Cascade)`
  - [ ] Adicionar model `ComissaoRegra` com `profissionalId String @unique` e `onDelete: Cascade`
  - [ ] Adicionar model `FechamentoCaixa` com `data DateTime @unique`
  - [ ] Em `Agendamento`: adicionar `valorCobrado Float?`, adicionar `pagamentos Pagamento[]`, atualizar comentário do `status`
  - [ ] Em `Profissional`: adicionar `comissaoPercentual Float @default(0)` e `comissaoRegra ComissaoRegra?`
- [ ] **Task 2 — Gerar migração** (AC: 5, 7)
  - [ ] Rodar `npx prisma migrate dev --name epic1_financeiro`
  - [ ] Conferir o SQL gerado: apenas `CREATE TABLE` / `ALTER TABLE ADD COLUMN`, nenhum `DROP` ou recriação de tabela existente
  - [ ] Adicionar script `"db:migrate": "prisma migrate dev"` em `package.json`
- [ ] **Task 3 — Atualizar `prisma/seed.ts`** (AC: 6)
  - [ ] Após criar profissionais, criar `ComissaoRegra` para pelo menos Ana (ex.: 40) e Bruno (ex.: 35); espelhar em `Profissional.comissaoPercentual`
  - [ ] Converter o agendamento de hoje (Maria/Ana/Corte) para `status: "CONCLUIDO"`, `valorCobrado: 80`
  - [ ] Criar 1–2 `Pagamento` cuja soma == `valorCobrado` (ex.: `{ valor: 80, formaPagamento: "PIX" }`)
  - [ ] Adicionar `prisma.pagamento.deleteMany()`, `prisma.comissaoRegra.deleteMany()`, `prisma.fechamentoCaixa.deleteMany()` no bloco de limpeza (antes dos deletes existentes por causa das FKs)
- [ ] **Task 4 — Validar** (AC: 8)
  - [ ] `npx prisma validate`
  - [ ] `npm run db:reset` (aplica schema + seed limpo)
  - [ ] `npm run build`

## Dev Notes

### Contexto de arquitetura (fonte: `docs/architecture.md` §2)

DDL alvo (não copiar cega — conferir nomes com o schema atual):

```prisma
model Pagamento {
  id             String   @id @default(cuid())
  agendamento    Agendamento @relation(fields: [agendamentoId], references: [id], onDelete: Cascade)
  agendamentoId  String
  valor          Float
  formaPagamento String   // DINHEIRO | PIX | DEBITO | CREDITO
  dataHora       DateTime @default(now())
  conferido      Boolean  @default(false)
  criadoEm       DateTime @default(now())
  @@index([dataHora])
  @@index([agendamentoId])
}

model ComissaoRegra {
  id             String   @id @default(cuid())
  profissional   Profissional @relation(fields: [profissionalId], references: [id], onDelete: Cascade)
  profissionalId String   @unique
  percentual     Float
  criadoEm       DateTime @default(now())
}

model FechamentoCaixa {
  id              String   @id @default(cuid())
  data            DateTime @unique
  totalDinheiro   Float
  totalPix        Float
  totalDebito     Float
  totalCredito    Float
  totalGeral      Float
  qtdAtendimentos Int
  ticketMedio     Float
  observacoes     String?
  criadoEm        DateTime @default(now())
}
```

### Decisões relevantes

- **Manter `ComissaoRegra` E `Profissional.comissaoPercentual`** (espelho denormalizado). A regra é
  canônica; o campo no profissional é para exibição rápida na lista e é atualizado pela mesma action
  na Story 1.3. (Arquitetura §2.1). Se o time decidir simplificar e remover `ComissaoRegra`, registrar
  aqui no Change Log — mas a recomendação é manter, para não pagar migração na fase 2.
- **Status permanece `String`** — não criar enum Prisma (evita migração de tipo). Os literais serão
  centralizados em `src/lib/financeiro.ts` na Story 1.2.
- **`prisma migrate dev`**: o projeto hoje só usa `db:push`. Esta story faz a transição uma vez.
  `db:reset` e `db:seed` continuam válidos; `db:push` pode continuar existindo para hotfix local.

### Relevant Source Tree

- `prisma/schema.prisma` — models atuais: `Servico`, `Profissional`, `Cliente`, `Agendamento`
- `prisma/seed.ts` — limpa tudo com `deleteMany()` e recria; ordem importa por causa de FKs
- `package.json` — scripts `db:push`, `db:seed`, `db:reset`, `build` (`prisma generate && next build`)
- `src/lib/prisma.ts` — singleton do `PrismaClient` (não muda)

### Fora de escopo

Nenhuma server action, componente ou tela nesta story. Só schema + migração + seed.

### Testing

- **Sem testes automatizados nesta story** (o runner Vitest entra na Story 1.2).
- Verificação manual:
  1. `npm run db:reset` termina sem erro e imprime "Seed concluído."
  2. Abrir `prisma/dev.db` (ou `npx prisma studio`) e confirmar: tabelas `Pagamento`, `ComissaoRegra`,
     `FechamentoCaixa` criadas; agendamento de hoje com `status = CONCLUIDO` e `valorCobrado = 80`;
     soma dos `Pagamento` desse agendamento == 80.
  3. `npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-migrations prisma/migrations` sem diferença pendente.
  4. `npm run build` verde.

## Change Log

| Date       | Version | Description                  | Author       |
|------------|---------|------------------------------|--------------|
| 2026-09-09 | 0.1     | Draft inicial da story       | River (@sm)  |
| 2026-09-09 | 1.0     | Validada — GO, Draft → Approved | Pax (@po) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex)

### Completion Notes List

- Schema estendido: `Pagamento`, `ComissaoRegra`, `FechamentoCaixa`; `Agendamento.valorCobrado Float?` + `pagamentos`; `Profissional.comissaoPercentual Float @default(0)` + `comissaoRegra`.
- Projeto não tinha histórico de migração (só `db push`). Criada baseline `00000000000000_init` (via `migrate diff --from-empty --to-url`, marcada `--applied`) e depois `20260909143404_epic1_financeiro` — esta é aditiva: `ADD COLUMN valorCobrado`, `CREATE TABLE` das 3 novas, e um table-rebuild do `Profissional` (padrão SQLite do Prisma para adicionar coluna com default) que preserva dados via `INSERT ... SELECT`.
- `package.json`: `"db:migrate": "prisma migrate dev"`.
- Seed: 2 `ComissaoRegra` (Ana 40, Bruno 35) espelhadas em `comissaoPercentual`; 1 agendamento hoje `CONCLUIDO` `valorCobrado 80` + `Pagamento` PIX 80; +1 agendamento AGENDADO hoje e o de coloração amanhã.
- `npx prisma validate`, `npm run db:seed`, `npm run build` — todos verdes.

### File List

- `prisma/schema.prisma` (mod)
- `prisma/seed.ts` (mod)
- `prisma/migrations/migration_lock.toml` (novo)
- `prisma/migrations/00000000000000_init/migration.sql` (novo)
- `prisma/migrations/20260909143404_epic1_financeiro/migration.sql` (novo)
- `package.json` (mod)

## QA Results
