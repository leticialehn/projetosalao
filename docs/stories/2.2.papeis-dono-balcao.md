# Story 2.2: Papéis dono e balcão

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** que o operador de balcão não consiga alterar comissões, taxas ou apagar cadastros,
**so that** mudanças sensíveis (dinheiro e dados mestres) fiquem só comigo.

## Acceptance Criteria

1. `Usuario.papel` já existe (`"DONO" | "BALCAO"`, default `"DONO"` — criado na Story 2.1). Esta story passa a **usar** esse campo para autorizar.
2. Existe uma tela `/usuarios` (**acesso só DONO**) para: listar usuários, criar um novo (usuário + senha + papel), trocar a senha de um usuário e remover um usuário. Não é possível remover o último usuário `DONO` nem rebaixar o último `DONO` para `BALCAO`.
3. **Ações restritas ao DONO** (checagem no servidor, na própria server action — não só escondendo o botão):
   - definir percentual de comissão (`definirComissao`)
   - excluir serviço, profissional ou cliente (`excluirServico`, `excluirProfissional`, `excluirCliente`)
   - reabrir um caixa já fechado (`reabrirCaixa`)
   - qualquer ação de `/usuarios`
4. O **BALCÃO** pode: ver e usar a agenda; criar / concluir / remarcar / cancelar / marcar falta em atendimentos; **fechar** o caixa do dia corrente (`fecharCaixa`); ver painel; ver a ficha do cliente e editar as observações; criar e editar cliente. Não pode as ações do AC 3.
5. Uma ação negada por papel retorna `{ ok: false, erro: "Ação permitida apenas para o dono." }` (ou, nas ações fire-and-forget, não executa e não quebra a tela) — **e a operação não acontece**.
6. O layout esconde do BALCÃO os controles que ele não pode usar: link "Usuários" e link "Profissionais" no `Nav` (edição de comissão vive lá), botões "Excluir" nas listas de serviço/profissional/cliente, o campo de comissão em `ProfissionalRow`, e o botão "Reabrir" no caixa.
7. A barra lateral mostra o papel do usuário logado ("Dono" / "Balcão") junto do nome.
8. Testes: `exigirPapel` (unit) + pelo menos um teste garantindo que uma server action restrita **rejeita** quando a sessão é de um `BALCAO`. `npm run test`, `npm run lint`, `npm run build` verdes. Sem migração (o campo já existe).

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Segurança / Autorização (RBAC simples) + Frontend (condicional por papel)
**Secondary Type(s)**: API (server actions), pequena tela CRUD de usuários
**Complexity**: Baixa-média

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco em autorização no servidor)

**Supporting Agents**:
- @architect (referência: padrão de sessão da Story 2.1)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando o Epic 2 for para PR
- [ ] Pre-Deployment: N/A (sem nova variável de ambiente)

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (ação restrita executável por BALCAO): auto_fix
- HIGH: document_as_debt se persistir

### CodeRabbit Focus Areas

**Primary Focus**:
- Autorização **no servidor** em toda ação restrita — nunca confiar só em esconder o botão
- Não dá para o BALCÃO se auto-promover (a ação de mudar papel é DONO-only e protege o "último DONO")
- Mensagem de erro consistente em pt-BR

**Secondary Focus**:
- `exigirPapel` reaproveita `exigirSessao` (Story 2.1) — sem duplicar leitura de sessão
- Condicionais de UI por papel não vazam dados (o BALCÃO nem recebe o componente, não só `display:none`)
- `/usuarios` nunca expõe hash de senha no HTML

## Tasks / Subtasks

- [ ] **Task 1 — Helper de autorização** (AC: 3, 5) em `src/lib/sessao.ts`
  - [ ] `exigirPapel(...papeis: string[])`: chama `exigirSessao()`, e se `sessao.papel` não estiver em `papeis` → `redirect("/login")` **ou** lançar um erro tratável; decidir o padrão (ver Dev Notes) e usar consistentemente
  - [ ] Alternativa para actions que retornam `{ ok, erro }`: `podeOuErro(papel)` que devolve `{ ok:false, erro:"Ação permitida apenas para o dono." }` para o caller retornar
- [ ] **Task 2 — Aplicar restrição de papel nas actions** (AC: 3)
  - [ ] `definirComissao` (`profissionais/actions.ts`) → DONO
  - [ ] `excluirServico` (`servicos/actions.ts`), `excluirProfissional` (`profissionais/actions.ts`), `excluirCliente` (`clientes/actions.ts`) → DONO
  - [ ] `reabrirCaixa` (`caixa/actions.ts`) → DONO. `fecharCaixa` continua liberado ao BALCÃO
  - [ ] Todas já têm `exigirSessao()` (Story 2.1) — trocar/estender para `exigirPapel("DONO")`
- [ ] **Task 3 — Tela `/usuarios`** (AC: 2) — dentro de `src/app/(app)/usuarios/`
  - [ ] `page.tsx` (Server Component): `exigirPapel("DONO")` no topo; lista `Usuario` (sem `senhaHash`)
  - [ ] `actions.ts`: `criarUsuario` (usuario, senha ≥ 8, papel), `trocarSenhaUsuario` (id, senha), `mudarPapelUsuario` (id, papel), `removerUsuario` (id). Todas `exigirPapel("DONO")`. Reutiliza `bcrypt.hash` (cost 10) da Story 2.1
  - [ ] Guarda do "último DONO": `removerUsuario` e `mudarPapelUsuario` recusam se resultariam em zero `DONO`
  - [ ] Client component leve para os formulários (padrão `useActionState` / `{ ok, erro }`)
- [ ] **Task 4 — UI condicional por papel** (AC: 6, 7)
  - [ ] `(app)/layout.tsx` já tem a sessão; passar `papel` para `Nav` e `Sair`
  - [ ] `Nav.tsx`: esconder "Usuários" e "Profissionais" quando `papel !== "DONO"` (a lista de profissionais é read-only útil? decidir — ver Dev Notes; por ora esconder)
  - [ ] `Sair.tsx`: mostrar "Dono" / "Balcão"
  - [ ] `ServicoRow`, `ProfissionalRow`, `ClienteRow`: receber `podeDono: boolean` (ou ler via contexto) e não renderizar o `DeleteButton` / campo de comissão quando falso
  - [ ] Caixa (`FecharCaixa.tsx` / `caixa/page.tsx`): esconder "Reabrir" para BALCÃO
- [ ] **Task 5 — Seed** (AC: 2)
  - [ ] `prisma/seed-admin.ts` continua criando só o DONO. Opcional: aceitar `BALCAO_USER`/`BALCAO_PASSWORD` para criar um balcão de exemplo em dev (documentar; não obrigatório)
- [ ] **Task 6 — Testes** (AC: 8)
  - [ ] `src/lib/sessao.test.ts`: `exigirPapel("DONO")` com sessão BALCAO → bloqueia; com DONO → passa
  - [ ] Teste de uma action restrita (ex.: `definirComissao`) rejeitando sessão BALCAO sem gravar
- [ ] **Task 7 — Validar**: `npm run test && npm run lint && npm run build` + teste manual

## Dev Notes

### Contexto (Story 2.1)

- `src/lib/sessao.ts`: `exigirSessao()` retorna `{ usuarioId, usuario, papel }` e faz `redirect("/login")` se não houver sessão. `src/lib/sessao-config.ts` tem a config Edge-safe.
- Toda server action mutadora de `(app)` **já chama `await exigirSessao()`** na primeira linha (Story 2.1). Esta story troca essas chamadas por `exigirPapel(...)` onde há restrição, e mantém `exigirSessao()` nas demais.
- `Usuario` model: `id`, `usuario @unique`, `senhaHash`, `papel @default("DONO")`, `criadoEm`. **Sem migração nesta story.**
- `bcryptjs` já é dependência; `bcrypt.hash(senha, 10)` é o padrão (ver `prisma/seed-admin.ts`).
- `Nav.tsx` é `"use client"` (usa `usePathname`); recebe props do layout server.
- Rows (`ServicoRow`, `ProfissionalRow`, `ClienteRow`) são client components renderizados pelas `page.tsx` server.

### Decisões a tomar (registrar no Change Log)

1. **`exigirPapel` — redirect ou erro?** Para ações fire-and-forget (retornam `void`), `redirect("/login")` é abrupto mas simples. Para ações `{ ok, erro }`, melhor retornar o erro e deixar a tela mostrar. Recomendação: `exigirPapel` lança um `ForbiddenError` custom; um pequeno wrapper nas actions `{ ok, erro }` captura e devolve `{ ok:false, erro:"Ação permitida apenas para o dono." }`; nas `void`, deixa propagar (Next mostra erro) OU retorna silenciosamente. Definir e padronizar.
2. **BALCÃO vê `/profissionais`?** A tela mistura dados úteis (telefone, especialidade) com a edição de comissão. Opções: (a) esconder a rota inteira do BALCÃO; (b) mostrar read-only sem o campo de comissão e sem excluir. Recomendação inicial: **(a) esconder** — simples; reabrir como melhoria se o balcão precisar consultar contatos.
3. **Editar serviço/profissional (nome, preço) — DONO-only também?** O PRD lista só *excluir* como restrito. Preço afeta dinheiro. Recomendação: manter conforme PRD (editar = liberado) nesta story e abrir a pergunta para o @po; se ele quiser restringir, é trivial adicionar.

### Assunções explícitas

- RBAC de 2 papéis fixos, sem permissões granulares. `papel` é string (consistente com `status` do Epic 1).
- Sem tela de "meu perfil" / trocar a própria senha nesta story (o DONO troca por `/usuarios`; o BALCÃO pede ao DONO). Pode virar follow-up.
- Esconder controle na UI é conveniência; a verdade é a checagem no servidor (AC 3/5).

### Edge cases

- Último DONO: não pode ser removido nem rebaixado (AC 2).
- BALCÃO acessa `/usuarios` digitando a URL → `exigirPapel("DONO")` redireciona.
- BALCÃO dispara a action restrita direto (POST) → rejeitada no servidor (mesma proteção da Story 2.1, agora com papel).
- Criar usuário com `usuario` já existente → `{ ok:false, erro:"Já existe um usuário com esse nome." }`.

### Testing

- **Unit:** `exigirPapel` (Vitest, com o mesmo mock de sessão de `sessao.test.ts`).
- **Ação restrita:** `definirComissao` com sessão BALCAO → erro/reject, `ComissaoRegra` inalterada.
- **Manual:**
  1. Criar um usuário `caixa1` / papel BALCÃO em `/usuarios` (logado como dono).
  2. Sair, entrar como `caixa1`: `Nav` sem "Usuários"/"Profissionais"; sem botões "Excluir"; sem "Reabrir" no caixa; barra lateral mostra "Balcão".
  3. Como `caixa1`: concluir um atendimento e fechar o caixa do dia → funciona.
  4. Como `caixa1`, abrir `/usuarios` na URL → volta para `/login`.
  5. Tentar remover o único DONO → recusado.

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-10 | 0.1     | Draft inicial da story | River (@sm)  |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
