# Story 6.1: Papel PROFISSIONAL e vínculo com o cadastro de profissional

## Status

InReview

## PO Validation (2026-09-17 — Pax)

**Verdict: GO** — Implementation Readiness 9/10. Sem dependências pendentes (Epic 2 Done); migração aditiva em `Usuario` apenas.

| Categoria | Status | Observações |
|---|---|---|
| 1. Template completeness | PASS | Todas as seções do template presentes (Status, Executor Assignment, Story, AC, CodeRabbit, Tasks/Subtasks, Dev Notes + Testing, Change Log, Dev Agent Record, QA Results); nenhum placeholder sem preencher |
| 2. Executor Assignment | PASS | `executor: "@dev"`, `quality_gate: "@qa"` — diferentes entre si; `quality_gate_tools` não vazio |
| 3. File structure / source tree | PASS | Caminhos exatos citados e conferidos contra o código atual (`prisma/schema.prisma`, `src/lib/sessao.ts`, `src/app/(app)/usuarios/{actions.ts,page.tsx,Usuarios.tsx}`, `src/app/(app)/profissionais/actions.ts`) |
| 4. AC coverage pelas tasks | PASS | 7 ACs (após correção — ver abaixo), todas mapeadas em pelo menos uma task (Tasks 1–7) |
| 5. Testing guidance | PASS | Task 6 cobre `criarUsuario`/`mudarPapelUsuario` isoladamente; Task 3 adiciona teste de regressão em `profissionais/actions.test.ts` pro novo comportamento de `excluirProfissional` |
| 6. Anti-alucinação | PASS | Toda claim técnica reverificada linha a linha contra o código atual: `sessao.ts` (papéis existentes), `usuarios/actions.ts` (guarda `soDono`, `papelValido` compartilhado, forma exata de `criarUsuario`/`mudarPapelUsuario`), `usuarios/Usuarios.tsx` (estrutura do formulário, `LABEL`), `schema.prisma` (ausência de relação em `Usuario`, precedente de campo opcional em `ComissaoRegra.servicoId`), `profissionais/actions.ts` (`excluirProfissional` faz `delete` físico quando `emUso === 0` — confirmado antes de propor a correção da AC 7) |
| 7. CodeRabbit Integration | PASS | Completa desde a criação — Story Type, agentes, quality gates, self-healing, focus areas |

### Correção aplicada nesta validação (mecânica, sem alterar escopo)

Draft original usava `onDelete: SetNull` na relação `Usuario.profissionalId → Profissional`. Isso permitiria que o `excluirProfissional` já existente (exclusão física quando o profissional tem zero agendamentos) criasse, depois do fato, exatamente o estado que a própria AC 2 do draft dizia ser impossível: um `Usuario` com papel `PROFISSIONAL` e `profissionalId` nulo. Corrigido para `onDelete: Restrict` + nova **AC 7** + **Task 3**, que ensinam `excluirProfissional` a inativar (em vez de excluir) quando há um login vinculado — mesmo padrão que a action já usa pra `emUso > 0`. Sem essa correção, a action existente quebraria com um erro de FK não tratado assim que alguém tentasse excluir um profissional com login. Tasks renumeradas (3→7) para acomodar a nova Task 3.

### Observação da PO

A decisão de deixar conversão de papel (`mudarPapelUsuario` para/de `PROFISSIONAL`) fora desta fatia está bem justificada no Dev Notes e é consistente com o padrão YAGNI já usado em stories anteriores do projeto — não é uma lacuna, é escopo deliberado. Nenhuma correção adicional necessária.

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** criar um login para um profissional específico, vinculado ao cadastro dele,
**so that** cada profissional possa futuramente acessar o sistema com sua própria conta, restrita ao que é dele.

## Acceptance Criteria

1. Novo papel `PAPEL_PROFISSIONAL = "PROFISSIONAL"` exportado por `src/lib/sessao.ts`, ao lado de `PAPEL_DONO`/`PAPEL_BALCAO` já existentes.
2. `Usuario` ganha um campo opcional `profissionalId` (relação um-para-um com `Profissional`, `onDelete: Restrict`) — migração aditiva, nenhuma tabela/coluna existente é alterada. Um `Usuario` com papel `PROFISSIONAL` e `profissionalId` nulo é um estado inválido que o sistema deve recusar a criar — e `onDelete: Restrict` (em vez de `SetNull`) garante que ele também não possa *aparecer depois*, via exclusão do `Profissional` vinculado (ver AC 7).
3. Em `criarUsuario` (`src/app/(app)/usuarios/actions.ts`): quando `papel === PAPEL_PROFISSIONAL`, o `profissionalId` enviado é obrigatório e validado no servidor — o profissional referenciado precisa existir, estar `ativo` e **ainda não ter nenhum outro `Usuario` vinculado** (mensagem clara: "Este profissional já tem um login."). Para `papel` `DONO`/`BALCAO`, o comportamento é idêntico ao atual (nenhum `profissionalId` é lido nem gravado).
4. `mudarPapelUsuario` continua aceitando somente `DONO`/`BALCAO` — converter um usuário existente de/para `PROFISSIONAL` fica fora desta fatia (ver Dev Notes). Uma chamada dessa action com `papel=PROFISSIONAL` é rejeitada com erro explícito ("Papel inválido."), não ignorada silenciosamente.
5. Tela `/usuarios`: o formulário "Novo usuário" ganha a opção "Profissional" no seletor de papel; ao selecioná-la, aparece um segundo campo para escolher **qual** profissional (lista apenas profissionais `ativo: true` que ainda não têm usuário vinculado). A listagem de usuários mostra o rótulo "Profissional" e, quando houver vínculo, o nome do profissional correspondente.
6. `npm run test`, `npm run lint` e `npm run build` continuam verdes.
7. `excluirProfissional` (`src/app/(app)/profissionais/actions.ts`, já existente) passa a tratar "tem um `Usuario` vinculado" da mesma forma que já trata "tem agendamento" hoje: em vez de excluir fisicamente, inativa (`ativo: false`). Sem essa mudança, `onDelete: Restrict` (AC 2) faria essa action existente quebrar com um erro de banco não tratado ao tentar excluir um profissional com login vinculado e zero agendamentos.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Database (novo campo/relação em `Usuario`) + Security (novo papel de acesso)
**Secondary Type(s)**: Frontend (formulário de criação de usuário)
**Complexity**: Média — não é um CRUD simples: introduz uma relação um-para-um com validação de unicidade "de negócio" (um profissional só pode ter um login) além da unicidade de banco, e um estado que precisa ser impossível de criar (papel PROFISSIONAL sem vínculo)

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco na impossibilidade de criar `PROFISSIONAL` sem `profissionalId` válido e único)

**Supporting Agents**:
- @data-engineer (referência: relação um-para-um `Usuario.profissionalId` → `Profissional`, `onDelete: SetNull`)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando for pra PR
- [ ] Pre-Deployment: `prisma migrate deploy` já roda no start (Railway); sem passo extra — coluna nova, nullable, sem dado existente afetado

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (migração destrutiva, ou `mudarPapelUsuario` aceitando `PROFISSIONAL` sem vínculo): auto_fix
- HIGH (mensagem de erro genérica em vez de "Este profissional já tem um login."): auto_fix se persistir, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Impossível persistir `Usuario` com `papel === PROFISSIONAL` e `profissionalId` nulo ou de um profissional já vinculado/inativo
- `mudarPapelUsuario` rejeita explicitamente `PROFISSIONAL` (não é um "esquecimento" — é a decisão de escopo desta fatia)

**Secondary Focus**:
- Guarda `papel !== PAPEL_DONO` já existente em `soDono()` continua cobrindo todas as actions de `/usuarios`, sem regressão
- Nenhuma tabela existente além de `Usuario` é alterada pela migração
- `excluirProfissional` (`profissionais/actions.ts`) não pode lançar um erro de constraint de banco não tratado ao tentar excluir um profissional com login vinculado — precisa cair no ramo de inativação antes de chegar no `delete`

## Tasks / Subtasks

- [x] **Task 1 — Schema e constante de papel** (AC: 1, 2) em `prisma/schema.prisma` e `src/lib/sessao.ts`
  - [x] `src/lib/sessao.ts`: adicionar `export const PAPEL_PROFISSIONAL = "PROFISSIONAL";`
  - [x] `prisma/schema.prisma`: `Usuario` ganha `profissionalId String? @unique` + `profissional Profissional? @relation(fields: [profissionalId], references: [id], onDelete: Restrict)`; `Profissional` ganha o lado reverso `usuario Usuario?` (só para permitir consultar "profissionais sem usuário" via `where: { usuario: null }`)
  - [x] `npx prisma migrate dev --name epic6_papel_profissional` — conferido: só `CREATE TABLE new_Usuario` (rebuild padrão do SQLite pra adicionar coluna+FK) com `INSERT...SELECT` preservando as linhas existentes; nenhuma tabela existente perde dado
- [x] **Task 2 — Actions** (AC: 3, 4) em `src/app/(app)/usuarios/actions.ts`
  - [x] `papelValido` (usado hoje por `criarUsuario` e `mudarPapelUsuario`) vira duas checagens: uma que aceita `DONO | BALCAO | PROFISSIONAL` (para `criarUsuario`) e outra que aceita só `DONO | BALCAO` (para `mudarPapelUsuario`) — não reaproveitar a mesma função para as duas actions, já que agora têm regras diferentes
  - [x] `criarUsuario`: quando `papel === PAPEL_PROFISSIONAL`, ler `profissionalId` do formData; validar (nessa ordem, retornando o primeiro erro encontrado): campo presente → profissional existe → profissional está `ativo` → profissional não tem `usuario` vinculado ainda. Só então criar o `Usuario` com `profissionalId` preenchido
  - [x] `mudarPapelUsuario`: se `papel === PAPEL_PROFISSIONAL`, retornar `{ ok: false, erro: "Papel inválido." }` sem tocar no banco
- [x] **Task 3 — Proteger exclusão de profissional vinculado** (AC: 7) em `src/app/(app)/profissionais/actions.ts`
  - [x] `excluirProfissional`: antes de decidir entre `delete` e `update({ativo:false})`, também checar `prisma.usuario.count({ where: { profissionalId: id } })`; se > 0, inativar em vez de excluir (mesmo ramo já usado quando `emUso > 0`)
  - [x] Teste novo em `src/app/(app)/profissionais/actions.test.ts` (arquivo já existente): profissional com `Usuario` vinculado e zero agendamentos → `excluirProfissional` inativa em vez de excluir
- [x] **Task 4 — Página: dados para o formulário** (AC: 5) em `src/app/(app)/usuarios/page.tsx`
  - [x] Buscar `profissionaisDisponiveis = await prisma.profissional.findMany({ where: { ativo: true, usuario: null }, select: { id: true, nome: true }, orderBy: { nome: "asc" } })` e passar para `<Usuarios />`
  - [x] Incluir `profissionalId` e o nome do profissional vinculado (join simples) na seleção de `usuarios` já existente, para exibir na listagem
- [x] **Task 5 — Formulário e listagem** (AC: 5) em `src/app/(app)/usuarios/Usuarios.tsx`
  - [x] Seletor de papel do formulário "Novo usuário" ganha a opção `<option value="PROFISSIONAL">Profissional</option>`
  - [x] Quando `papel === "PROFISSIONAL"` estiver selecionado (estado local do form), renderizar um `<select name="profissionalId">` com `profissionaisDisponiveis`; se a lista estiver vazia, mostrar aviso "Nenhum profissional ativo disponível para vincular." e desabilitar o submit
  - [x] `LABEL` ganha `PROFISSIONAL: "Profissional"`; linha da listagem mostra o nome do profissional vinculado (quando houver) ao lado do badge de papel
- [x] **Task 6 — Testes** (AC: 3, 4, 6) em `src/app/(app)/usuarios/actions.test.ts` (novo arquivo)
  - [x] `criarUsuario` com `papel: "PROFISSIONAL"` sem `profissionalId` → erro, nenhuma escrita
  - [x] `criarUsuario` com `profissionalId` de um profissional inativo → erro, nenhuma escrita
  - [x] `criarUsuario` com `profissionalId` já vinculado a outro usuário → erro ("Este profissional já tem um login."), nenhuma escrita
  - [x] `criarUsuario` com `profissionalId` válido e disponível → cria com sucesso, `profissionalId` gravado
  - [x] `mudarPapelUsuario` com `papel: "PROFISSIONAL"` → erro, nenhuma escrita (AC 4)
  - [x] Guarda `papel !== PAPEL_DONO` (`soDono()`) continua coberta pelos testes já existentes do padrão do projeto (chamada por não-dono não escreve)
- [x] **Task 7 — Validar**: `npm run test && npm run lint && npm run build`

## Dev Notes

### Contexto (arquivos relevantes já existentes)

- `src/lib/sessao.ts`: hoje exporta só `PAPEL_DONO`/`PAPEL_BALCAO`, `exigirSessao()`, `exigirPapel(...papeis)`. `[Source: src/lib/sessao.ts]`
- `src/app/(app)/usuarios/actions.ts`: `criarUsuario`, `trocarSenhaUsuario`, `mudarPapelUsuario`, `removerUsuario` — todas começam com `soDono()` (guarda `papel !== PAPEL_DONO`). `papelValido(p)` hoje é `p === PAPEL_DONO || p === PAPEL_BALCAO`, compartilhada entre `criarUsuario` e `mudarPapelUsuario`. **Esta story quebra esse compartilhamento** (Task 2) porque as duas actions passam a ter regras diferentes de papel válido. `[Source: src/app/(app)/usuarios/actions.ts]`
- `src/app/(app)/usuarios/Usuarios.tsx`: formulário client component com `useActionState`; `LABEL` é um `Record<string,string>` só com `DONO`/`BALCAO` hoje. `[Source: src/app/(app)/usuarios/Usuarios.tsx]`
- `prisma/schema.prisma`: `Usuario` hoje não tem nenhuma relação com outra tabela (`id`, `usuario`, `senhaHash`, `papel`, `criadoEm`). `Profissional` não tem nenhuma referência de volta a `Usuario`. `[Source: prisma/schema.prisma]`
- Padrão de campo opcional com relação um-para-um já existe no projeto em `ComissaoRegra.servicoId` (`String?` + relação opcional) — mesma forma de declarar. `[Source: prisma/schema.prisma]`

### Por que a validação de "profissional já tem login" não é só a constraint única do banco

A constraint `@unique` em `profissionalId` impede duas linhas de `Usuario` apontarem pro mesmo profissional, mas um erro de constraint do SQLite estourado direto pro usuário final é uma mensagem ruim ("UNIQUE constraint failed"). Por isso a Task 2 pede a checagem explícita **antes** do `create` — mesmo princípio já usado em `criarUsuario` hoje para "já existe um usuário com esse nome" (`prisma.usuario.findUnique({ where: { usuario } })` antes de criar).

### Por que `onDelete: Restrict` em vez de `SetNull`

`src/app/(app)/profissionais/actions.ts` já tem um `excluirProfissional` que faz exclusão física quando o profissional não tem nenhum agendamento (`emUso === 0`). Um profissional recém-vinculado a um login pode perfeitamente ter zero agendamentos ainda. Com `onDelete: SetNull`, o dono excluindo esse profissional deixaria pra trás um `Usuario` com `papel: PROFISSIONAL` e `profissionalId: null` — exatamente o estado que a AC 2 diz que não pode existir, só que criado por um caminho diferente da criação (uma exclusão, não um cadastro). `onDelete: Restrict` fecha esse caminho no nível do banco; a Task 3 ajusta `excluirProfissional` pra tratar "tem login vinculado" como mais um motivo de inativar em vez de excluir, ao lado do `emUso` já existente — sem essa mudança, a action existente quebraria com um erro de FK não tratado. `[Source: src/app/(app)/profissionais/actions.ts]`

### Por que conversão de papel (Task de mudarPapelUsuario) fica fora desta fatia

Converter um usuário `DONO`/`BALCAO` existente para `PROFISSIONAL` exigiria o mesmo formulário de escolha de profissional dentro da tela de edição de papel (`mudarPapelUsuario`), que hoje é só um `<select>` de duas opções sem campo adicional. Fazer isso corretamente (incluindo o caminho inverso: profissional deixando de ser profissional) é escopo suficiente pra uma fatia própria depois que a criação direta (esta story) estiver em uso — YAGNI até validar que a conversão é realmente necessária no dia a dia, em vez de simplesmente remover e recriar o usuário.

### Testing

- **Unit:** ver Task 5 — replica o padrão de mock de sessão/prisma já usado em `produtos/actions.test.ts` e `profissionais/actions.test.ts` (variável `PAPEL` controlável, `vi.mock("@/lib/prisma", ...)`).
- **Manual:** login como dono, abrir `/usuarios`, criar um profissional novo (se necessário) em `/profissionais`, depois criar um login com papel "Profissional" vinculado a ele; confirmar que esse mesmo profissional some da lista de opções ao tentar criar um segundo login apontando pra ele.

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-17 | 0.1     | Draft inicial — primeira story do Epic 6, conforme PRD | River (@sm) |
| 2026-09-17 | 1.0     | Validada GO (9/10) — corrigido `onDelete` de `SetNull` para `Restrict` + AC 7/Task 3 — Status: Draft → Ready | Pax (@po) |
| 2026-09-17 | 1.1     | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-09-17 | 1.2     | Development complete — Status: InProgress → InReview | @dev |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev, yolo mode)

### Completion Notes List

- `papelValido` (usado por `mudarPapelUsuario`) e a nova `papelCriacaoValido` (usada por `criarUsuario`) são funções separadas de propósito — `mudarPapelUsuario` continua rejeitando `PROFISSIONAL` com a mensagem exata "Papel inválido." pedida na AC 4.
- `criarUsuario` valida o profissional em três passos sequenciais (existe → ativo → sem usuário vinculado), cada um com mensagem própria, antes de checar duplicidade de nome de usuário — mesma ordem descrita na Task 2.
- Correção da PO (`onDelete: Restrict`) implementada como especificado: `excluirProfissional` agora consulta `prisma.usuario.count({ where: { profissionalId: id } })` ao lado do `emUso` já existente, e trata os dois como o mesmo motivo de inativação em vez de exclusão física.
- `Usuarios.tsx`: o campo de seleção de profissional só aparece quando "Profissional" está selecionado no papel (estado local `papelNovo`); com a lista de disponíveis vazia, o botão "Adicionar" fica desabilitado e uma mensagem explica o motivo, em vez de deixar o formulário submeter sem `profissionalId`.
- `usuarios/actions.test.ts` (novo) segue o mesmo padrão de mock de `@/lib/sessao`/`@/lib/prisma` já usado em `produtos/actions.test.ts`/`profissionais/actions.test.ts`; `bcryptjs` também mockado pra evitar hashing real nos testes.
- `profissionais/actions.test.ts` ganhou mocks de `agendamento.count`/`usuario.count`/`profissional.delete` (não existiam antes, só cobria `definirComissao`) e 3 testes novos cobrindo as três combinações de `excluirProfissional` (nenhum bloqueio, usuário vinculado, agendamento existente) — o terceiro é regressão do comportamento que já existia antes desta story.
- Gates verdes: `npm run test` (108/108, 9 novos), `npm run lint` (limpo), `npx tsc --noEmit` (limpo — corrigido um erro de indexação de tupla no mock de `createMock.mock.calls`, mesmo padrão já visto na Story 3.3), `npm run build` (compilado, todas as rotas incluindo `/usuarios`).
- Verificação manual interativa (criar de fato um login de profissional pela UI em `/usuarios`, com o servidor de dev rodando) não executada nesta sessão por falta de ferramenta de automação de navegador — recomendo ao @qa exercitar esse fluxo, incluindo o caso de a lista de profissionais disponíveis ficar vazia.

### File List

- `prisma/schema.prisma` (modificado — `Usuario.profissionalId`/relação, `Profissional.usuario` reverso)
- `prisma/migrations/20260917140000_epic6_papel_profissional/migration.sql` (criado)
- `src/lib/sessao.ts` (modificado — `PAPEL_PROFISSIONAL`)
- `src/app/(app)/usuarios/actions.ts` (modificado — `criarUsuario`, `mudarPapelUsuario`, `papelCriacaoValido`)
- `src/app/(app)/usuarios/actions.test.ts` (criado)
- `src/app/(app)/usuarios/page.tsx` (modificado — busca `profissionaisDisponiveis`, join do nome vinculado)
- `src/app/(app)/usuarios/Usuarios.tsx` (modificado — seletor de profissional condicional, `LABEL`, listagem)
- `src/app/(app)/profissionais/actions.ts` (modificado — `excluirProfissional` checa usuário vinculado)
- `src/app/(app)/profissionais/actions.test.ts` (modificado — mocks + 3 testes novos de `excluirProfissional`)

## QA Results

_A preencher pelo @qa após a implementação._
