# Story 9.1: Identificação de clientes inativos + envio de lembrete de retorno

## Status

Done

## PO Validation (2026-09-24 — Pax, revisão abreviada)

**Verdict: GO.** Regra de idempotência do ciclo de inatividade verificada manualmente (notificado antes
da última visita → elegível de novo; notificado depois → não reelegível até o próximo ciclo). Sem
migração destrutiva. Independente das Stories 10.1/11.1 — nenhum arquivo em comum além de
`src/lib/whatsapp.ts`, cujo suporte a `templateName` já foi adiantado numa mudança preparatória
separada, então as três stories podem ser implementadas em paralelo sem conflito de arquivo.

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** que o sistema avise sozinho os clientes que sumiram,
**so that** eu recupere receita de clientes inativos sem precisar lembrar de ligar pra cada um.

## Acceptance Criteria

1. Nova coluna `Cliente.ultimoLembreteRetencaoEm` (`DateTime?`, migração aditiva, nenhuma coluna existente é alterada) — marca quando o cliente recebeu o último lembrete de retorno, usada pra não notificar o mesmo ciclo de inatividade mais de uma vez.
2. Nova rota `GET /api/retencao` (fora do grupo `(app)`, sem sessão — protegida por `Authorization: Bearer <RETENCAO_CRON_SECRET>`, mesmo padrão de `/api/lembretes` da Story 8.2), pensada pra ser chamada por um Cron Job externo.
3. Um cliente é elegível quando: seu agendamento `CONCLUIDO` mais recente foi há mais de `RETENCAO_DIAS_INATIVIDADE` dias (env var, default 45); ele **não** tem nenhum agendamento `AGENDADO` (futuro); e (`ultimoLembreteRetencaoEm` é nulo **ou** é anterior à data desse último `CONCLUIDO`, ou seja, o cliente voltou e sumiu de novo desde o último lembrete — novo ciclo de inatividade). Cliente sem nenhum `CONCLUIDO` (nunca veio) nunca é elegível.
4. Pra cada cliente elegível, tenta e-mail e/ou WhatsApp (reusando `enviarEmail`/`enviarWhatsapp` do Epic 8, incluindo a mesma degradação graciosa sem configuração); `ultimoLembreteRetencaoEm` é gravado quando **pelo menos um** canal tem sucesso (mesmo critério da Story 8.3 pro lembrete de agendamento).
5. Sem `RETENCAO_CRON_SECRET` configurado, a rota responde **503**; com o secret configurado, uma chamada sem o header correto responde **401** — mesmo contrato de `/api/lembretes`.
6. `.env.example`/`DEPLOY.md` documentam `RETENCAO_CRON_SECRET`, `RETENCAO_DIAS_INATIVIDADE` (default 45) e o mesmo passo manual de Cron Job já documentado pra `/api/lembretes` (agora apontando também pra `/api/retencao`, com sua frequência sugerida: 1x/dia).
7. `npm run test`, `npm run lint` e `npm run build` continuam verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: API (nova rota) + Database (coluna aditiva)
**Secondary Type(s)**: Integration (reusa e-mail/WhatsApp já existentes, nenhuma integração nova)
**Complexity**: Baixa-média — replica a estrutura já validada de `/api/lembretes`; a única lógica genuinamente nova é a consulta de "cliente inativo, sem agendamento futuro"

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco em: um cliente com agendamento futuro nunca é notificado por engano; um cliente que voltou e o sistema não fica silenciosamente preso achando que ele ainda está no ciclo antigo)

**Supporting Agents**:
- @architect (referência: reuso do mesmo padrão de rota/autenticação de `/api/lembretes`)

### Quality Gate Tasks

- [x] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando for pra PR
- [x] Pre-Deployment: migração aditiva roda automaticamente; passo manual adicional de Cron Job documentado no `DEPLOY.md`

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (cliente com agendamento futuro recebendo lembrete de retorno por engano): auto_fix
- HIGH (mesmo cliente notificado repetidamente todo dia sem nunca ter voltado — falta de idempotência): auto_fix se persistir, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Regra "sem agendamento futuro" realmente exclui o cliente (não é só um `orderBy` que ignora o status)
- `enviarEmail`/`enviarWhatsapp` chamados exatamente como no `/api/lembretes` (sem reimplementar a lógica de canal)

**Secondary Focus**:
- Migração é só `ALTER TABLE Cliente ADD COLUMN` (nullable, sem default obrigatório)

## Tasks / Subtasks

- [x] **Task 1 — Schema** (AC: 1) em `prisma/schema.prisma`
  - [x] `Cliente` ganha `ultimoLembreteRetencaoEm DateTime?` (nullable, aditiva)
  - [x] `npx prisma migrate dev --name epic9_retencao` — conferir: só `ALTER TABLE Cliente ADD COLUMN`
- [x] **Task 2 — Template de mensagem** (AC: 4) em `src/lib/email.ts` e `src/lib/whatsapp.ts`
  - [x] `emailRetencao({ nome }: { nome: string }): { subject: string; html: string }` em `email.ts` — mesmo padrão de `emailLembrete`/`emailConfirmacao` (função `base()` já existente, `escapeHtml` no `nome`), mensagem simples tipo "sentimos sua falta, esperamos te ver em breve"
  - [x] WhatsApp: reusar `enviarWhatsapp` já existente com `parametros: [nome]` — **não** criar uma nova função de "template" em `whatsapp.ts` (o template em si é aprovado externamente no Meta Business Manager, como já é o caso pro lembrete); só documentar no `DEPLOY.md` (Task 4) que este é um **segundo** template (nome configurável por `WHATSAPP_TEMPLATE_RETENCAO_NAME`, variável nova) com **1 variável de corpo** (nome do cliente) — reusar `WHATSAPP_TEMPLATE_NAME` do lembrete de agendamento não funciona porque os templates da Meta têm contrato fixo de variáveis, e o de lembrete tem 4, não 1
  - [x] Em `enviarWhatsapp`, o nome do template já é lido de `process.env.WHATSAPP_TEMPLATE_NAME` — a rota de retenção (Task 3) precisa passar o nome do template de retenção de alguma forma. **Decisão**: adicionar um parâmetro opcional `templateName` a `enviarWhatsapp({ to, parametros, templateName? })`, usando `templateName ?? process.env.WHATSAPP_TEMPLATE_NAME` internamente — mudança mínima, compatível com todas as chamadas existentes (que não passam `templateName`) — **já implementado numa mudança preparatória anterior**, confirmado presente em `src/lib/whatsapp.ts` antes do início desta story, nenhuma alteração adicional necessária
- [x] **Task 3 — Rota de retenção** (AC: 2, 3, 4, 5) em `src/app/api/retencao/route.ts` (novo arquivo, mesma estrutura de `src/app/api/lembretes/route.ts`)
  - [x] Mesmo padrão de 503 sem `RETENCAO_CRON_SECRET`, 401 com header errado (copiar exatamente a lógica de `route.ts` do Epic 8, trocando o nome da env var)
  - [x] `const diasInatividade = Number(process.env.RETENCAO_DIAS_INATIVIDADE) || 45; const cutoff = new Date(Date.now() - diasInatividade * 24 * 60 * 60000);`
  - [x] `prisma.cliente.findMany({ include: { agendamentos: { where: { status: { in: ["CONCLUIDO", "AGENDADO"] } }, orderBy: { inicio: "desc" } } } })` — depois, em JS (mesmo padrão de `cliente.agendamentos.find(a => a.status === "CONCLUIDO")` já usado em `src/app/(app)/clientes/[id]/page.tsx:30`): pra cada cliente, `temFuturo = agendamentos.some(a => a.status === "AGENDADO")`; `ultimaVisita = agendamentos.find(a => a.status === "CONCLUIDO")`; elegível quando `!temFuturo && ultimaVisita && ultimaVisita.inicio < cutoff && (!cliente.ultimoLembreteRetencaoEm || cliente.ultimoLembreteRetencaoEm < ultimaVisita.inicio)`
  - [x] Pra cada cliente elegível: `emailOk`/`whatsappOk` seguindo exatamente o mesmo padrão condicional de `/api/lembretes` (Story 8.3) — e-mail só se `cliente.email`, WhatsApp só se `normalizarTelefoneBR(cliente.telefone)` não nulo, passando `templateName: process.env.WHATSAPP_TEMPLATE_RETENCAO_NAME` (Task 2); se `emailOk || whatsappOk`, gravar `ultimoLembreteRetencaoEm: new Date()`
  - [x] Retornar `{ enviados, pulados, total }` (mesmo formato de `/api/lembretes`)
  - [x] `src/middleware.ts`: adicionar `api/retencao` à mesma exceção do matcher, junto de `api/lembretes`
- [x] **Task 4 — Documentação** (AC: 6)
  - [x] `.env.example`: `RETENCAO_CRON_SECRET=`, `RETENCAO_DIAS_INATIVIDADE=45`, `WHATSAPP_TEMPLATE_RETENCAO_NAME=`
  - [x] `DEPLOY.md`: adicionar as variáveis à tabela existente; estender a seção "3c. Cron Job de lembretes" (ou criar uma "3e" equivalente) explicando que `/api/retencao` segue o mesmo padrão, sugerido 1x/dia; e a seção "3d. WhatsApp" ganha uma nota sobre o segundo template (retenção, 1 variável) precisar de aprovação separada da Meta
- [x] **Task 5 — Testes** (AC: 1, 3, 4, 5) em `src/app/api/retencao/route.test.ts` (novo, mesmo padrão de mocks de `route.test.ts` do Epic 8)
  - [x] Cliente com última visita `CONCLUIDO` há mais de 45 dias, sem agendamento futuro, nunca notificado → elegível, `enviarEmail`/`enviarWhatsapp` chamados, `ultimoLembreteRetencaoEm` gravado
  - [x] Cliente com agendamento `AGENDADO` futuro, mesmo com última visita antiga → **não** elegível, nenhum canal chamado
  - [x] Cliente sem nenhum `CONCLUIDO` → não elegível
  - [x] Cliente já notificado (`ultimoLembreteRetencaoEm` **depois** da última visita) → não elegível (evita spam)
  - [x] Cliente que voltou depois do último lembrete e sumiu de novo (`ultimoLembreteRetencaoEm` **antes** da última visita `CONCLUIDO`, e essa visita já passou do prazo) → elegível de novo (novo ciclo)
  - [x] Sem `RETENCAO_CRON_SECRET` → 503; header errado → 401 (mesmos casos do Epic 8, adaptados)
- [x] **Task 6 — Validar**: `npm run test && npm run lint && npm run build`

## Dev Notes

### Contexto (arquivos relevantes já existentes)

- `src/app/api/lembretes/route.ts` (Epic 8): estrutura completa a replicar — 503/401, `emailOk`/`whatsappOk` condicionados à própria disponibilidade, contagem `{ enviados, pulados, total }`. `[Source: src/app/api/lembretes/route.ts]`
- `src/app/(app)/clientes/[id]/page.tsx:30`: `cliente.agendamentos.find((a) => a.status === "CONCLUIDO")` — mesmo padrão de "achar a última visita concluída" a reusar na consulta de retenção. `[Source: src/app/(app)/clientes/[id]/page.tsx]`
- `src/lib/email.ts` / `src/lib/whatsapp.ts` (Epic 8): `enviarEmail`, `enviarWhatsapp`, `emailLembrete`/`emailConfirmacao` — modelo exato pra `emailRetencao` e pro parâmetro novo `templateName` de `enviarWhatsapp`. `[Source: src/lib/email.ts, src/lib/whatsapp.ts]`
- `src/middleware.ts`: matcher atual já exclui `login`, `agendar`, `api/lembretes` — esta story adiciona `api/retencao` à mesma lista.

### Por que um segundo template do WhatsApp (não reusar o de lembrete)

Templates da Meta têm um número fixo de variáveis aprovado — o de lembrete de agendamento (Story 8.3)
tem 4 (nome, serviço, profissional, data/hora); a mensagem de retenção não tem serviço/profissional/data
pra mostrar (é genérica: "sentimos sua falta"), então precisa de um template próprio, com 1 variável.
Daí o parâmetro `templateName` opcional em `enviarWhatsapp` — sem essa mudança, a Story 9.1 ficaria
travada usando o template errado (que exige 4 parâmetros e falharia na chamada à API da Meta).

### Testing

- **Unit:** ver Task 5. Seguir exatamente o padrão de mock de `@/lib/prisma`, `@/lib/email` e
  `@/lib/whatsapp` já usado em `src/app/api/lembretes/route.test.ts`.
- **Manual:** com um cliente de teste sem agendamento futuro e último `CONCLUIDO` mais de 45 dias atrás,
  chamar `curl -H "Authorization: Bearer <secret>" http://localhost:3000/api/retencao` e conferir o
  envio + `ultimoLembreteRetencaoEm` preenchido.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev, executando também como @qa nesta story)

### Completion Notes List

- Todas as 6 tasks implementadas literalmente conforme especificado nos Dev Notes — nenhuma
  decisão de design nova foi tomada, a lógica de elegibilidade e o formato de resposta seguem
  exatamente a fórmula já validada na PO Validation.
- `Cliente.ultimoLembreteRetencaoEm DateTime?` adicionada via `npx prisma migrate dev --name
  epic9_retencao`; migração gerada (`prisma/migrations/20260924183853_epic9_retencao/migration.sql`)
  confere: é só `ALTER TABLE "Cliente" ADD COLUMN "ultimoLembreteRetencaoEm" DATETIME;` — nenhuma
  coluna existente tocada.
- `emailRetencao({ nome })` em `src/lib/email.ts` reusa `base()` e `escapeHtml()` já existentes —
  mesmo tratamento contra HTML injection já aplicado em `emailLembrete`/`emailConfirmacao`.
- `enviarWhatsapp` já tinha o parâmetro opcional `templateName` (mudança preparatória anterior,
  confirmada por leitura de `src/lib/whatsapp.ts` antes de começar) — nenhuma alteração necessária
  nesse arquivo, só o uso do parâmetro na nova rota.
- `GET /api/retencao`: 503 sem `RETENCAO_CRON_SECRET`, 401 com header errado — idêntico ao
  contrato de `/api/lembretes`. Consulta busca todos os clientes com `agendamentos` filtrados a
  `CONCLUIDO`/`AGENDADO` (ordenados `desc` por `inicio`), e a elegibilidade é resolvida em JS com a
  fórmula exata do Dev Notes (`!temFuturo && ultimaVisita && ultimaVisita.inicio < cutoff &&
  (!ultimoLembreteRetencaoEm || ultimoLembreteRetencaoEm < ultimaVisita.inicio)`).
- `total` na resposta é a contagem de clientes elegíveis (`elegiveis.length`) — análogo a `total:
  agendamentos.length` em `/api/lembretes`, onde a lista já vem pré-filtrada pela query; aqui o
  filtro é feito em JS mas o significado de `total` (candidatos elegíveis nesta execução) é o
  mesmo.
- `src/middleware.ts`: `api/retencao` adicionado à mesma exceção do matcher que já tinha
  `api/lembretes`.
- `.env.example` e `DEPLOY.md` atualizados com `RETENCAO_CRON_SECRET`,
  `RETENCAO_DIAS_INATIVIDADE` (default 45), `WHATSAPP_TEMPLATE_RETENCAO_NAME`, nova seção "3e.
  Cron Job de retenção" (sugestão 1x/dia) e nota na seção "3d. WhatsApp" sobre o segundo template
  precisar de aprovação separada da Meta (1 variável de corpo vs. 4 do lembrete de agendamento).
- 11 testes novos em `src/app/api/retencao/route.test.ts`, cobrindo os 6 casos do Task 5 mais
  cenários adicionais (elegível sem canal disponível, nenhum canal com sucesso não grava o campo,
  `RETENCAO_DIAS_INATIVIDADE` customizado).
- **Self-QA (atuando como @qa):** revisão adversarial do diff antes de fechar a story, focada nos
  dois riscos que este tipo de rota já mostrou no passado neste projeto (chamada de
  `enviarEmail`/`enviarWhatsapp` com destinatário nulo, e regra de idempotência incorreta). Nenhum
  bug de código foi encontrado — `enviarEmail` só é chamado dentro do ramo `cliente.email ? ... :
  false` e `enviarWhatsapp` só dentro de `telefoneWhats ? ... : false`, então nenhum caminho chega
  a chamar essas funções com destinatário vazio/nulo. A regra de idempotência foi rastreada
  manualmente pelos 3 casos pedidos (nunca notificado → elegível; notificado depois da última
  visita → não elegível; notificado antes de uma visita mais nova e já vencida → elegível de novo)
  e bate com os testes escritos. **Um problema foi encontrado e corrigido, mas fora do código da
  story:** o primeiro `npx prisma migrate dev` rodou com um `.env` local mal configurado
  (`DATABASE_URL="file:./prisma/dev.db"`, caminho relativo duplicado porque o Prisma já resolve
  caminhos relativos a partir de `prisma/schema.prisma`), criando um `prisma/prisma/dev.db`
  perdido e sem uso — arquivo de banco local, gitignorado, sem nenhum efeito no código da
  aplicação. Corrigido apontando `.env` para `file:./dev.db` (mesma convenção do `.env.example`) e
  removendo o diretório espúrio antes de rodar os gates finais; `npm run test`, `npx tsc --noEmit`,
  `npm run lint` e `npm run build` foram todos re-executados depois da correção e continuam
  limpos.

### File List

- `prisma/schema.prisma` (modificado — `Cliente.ultimoLembreteRetencaoEm`)
- `prisma/migrations/20260924183853_epic9_retencao/migration.sql` (novo)
- `prisma/migrations/migration_lock.toml` (normalização de fim de linha, sem mudança de conteúdo)
- `src/lib/email.ts` (modificado — `emailRetencao`)
- `src/app/api/retencao/route.ts` (novo)
- `src/app/api/retencao/route.test.ts` (novo)
- `src/middleware.ts` (modificado — matcher inclui `api/retencao`)
- `.env.example` (modificado — variáveis de retenção)
- `DEPLOY.md` (modificado — seção 3e e nota na 3d)

## QA Results

**Gate: PASS**
**Reviewer:** Quinn (@qa), revisão independente pós-implementação (mesmo agente da sessão, atuando
no papel de QA conforme atribuição da story)
**Revisão de:** commit local (branch `feat/9.1-retencao-clientes`, antes do push)

### Checklist (7 verificações)

1. **Code review** — rota replica fielmente o padrão de `/api/lembretes`; nenhuma lógica de canal
   reimplementada; nomes e formato de resposta consistentes com o resto do projeto. OK.
2. **Unit tests** — 11 testes novos, todos os 6 cenários obrigatórios do Task 5 cobertos mais 5
   adicionais; suíte completa (191 testes) verde. OK.
3. **Acceptance criteria** — AC 1–7 conferidos individualmente:
   - AC1: coluna aditiva confirmada pelo SQL da migração. OK.
   - AC2: rota fora de `(app)`, sem sessão, protegida por Bearer secret. OK.
   - AC3: fórmula de elegibilidade rastreada manualmente (ver Completion Notes) e coberta por
     teste dedicado para cada ramo, incluindo o ciclo "voltou e sumiu de novo". OK.
   - AC4: `emailOk || whatsappOk` grava `ultimoLembreteRetencaoEm`; degradação graciosa herdada de
     `enviarEmail`/`enviarWhatsapp` (retornam `false` sem lançar quando não configurados). OK.
   - AC5: 503 sem secret, 401 com header errado — testado nos dois casos. OK.
   - AC6: `.env.example` e `DEPLOY.md` documentam as 3 variáveis novas e o Cron Job (1x/dia). OK.
   - AC7: `npm run test`, `npm run lint`, `npm run build` verdes (ver seção de validação abaixo).
4. **No regressions** — suíte pré-existente (180 testes de outras stories) permanece verde sem
   alteração; nenhum arquivo de outra story (10.1/11.1) tocado; único ponto de risco documentado
   pela PO (`src/lib/whatsapp.ts`) não precisou de nenhuma mudança adicional. OK.
5. **Performance** — consulta única (`findMany` com `include`) por execução do cron, sem N+1;
   volume esperado (base de clientes de um salão) é pequeno o suficiente pra não precisar de
   paginação nesta story. OK.
6. **Security** — `emailRetencao` escapa `nome` via `escapeHtml` (mesma defesa contra HTML
   injection já usada em `emailLembrete`/`emailConfirmacao`); autenticação Bearer idêntica ao
   padrão já auditado de `/api/lembretes`; nenhum dado sensível novo exposto na resposta JSON. OK.
7. **Documentation** — `.env.example`/`DEPLOY.md` atualizados; Dev Notes e File List desta story
   preenchidos. OK.

### Self-healing registrado

Nenhum bug de código precisou de correção nesta story. O único desvio encontrado durante a
revisão adversarial (Completion Notes) foi um artefato local de banco de dados
(`prisma/prisma/dev.db`) gerado por um `.env` de desenvolvimento mal configurado no início da
sessão — não é código da aplicação, não afeta a migração real (`prisma/migrations/`) nem os
testes, e foi limpo antes de fechar a story. Registrado aqui por transparência, não como
self-healing de um bug funcional.

### Riscos residuais / follow-ups (não bloqueiam PASS)

- O template de WhatsApp de retenção (`WHATSAPP_TEMPLATE_RETENCAO_NAME`) precisa ser criado e
  aprovado manualmente no Meta Business Manager antes do canal funcionar em produção — já
  documentado no `DEPLOY.md`, é um passo operacional fora do escopo de código desta story.
- O Cron Job externo (`GET /api/retencao` 1x/dia) precisa ser configurado manualmente no painel do
  Railway — mesmo modelo já em uso para `/api/lembretes`, documentado na seção 3e do `DEPLOY.md`.

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-24 | 0.1     | Draft inicial — primeira story do Epic 9, conforme PRD | River (@sm) |
| 2026-09-24 | 1.0     | Validada GO (revisão abreviada) — Status: Draft → Ready | Pax (@po) |
| 2026-09-24 | 1.1     | Início da implementação — Status: Ready → InProgress | Dex (@dev) |
| 2026-09-24 | 1.2     | Implementação completa (schema, template, rota, middleware, docs, 11 testes novos); gates `test`/`lint`/`build` verdes — Status: InProgress → InReview | Dex (@dev) |
| 2026-09-24 | 1.3     | QA Gate PASS — self-QA adversarial sem bugs de código encontrados (artefato local de banco limpo, sem impacto funcional); todos os checks de qualidade OK — Status: InReview → Done | Quinn (@qa) |
