# Story 7.3: Notificações de meta batida e incentivo individual

## Status

Ready

## PO Validation (2026-09-17 — Pax)

**Verdict: GO** — Implementation Readiness 9/10. Sem migração; depende só das Stories 7.1/7.2 (Done) e do Epic 6 (Done).

| Categoria | Status | Observações |
|---|---|---|
| 1. Template completeness | PASS | Todas as seções presentes; nenhum placeholder sem preencher |
| 2. Executor Assignment | PASS | `executor: "@dev"`, `quality_gate: "@qa"` — diferentes; `quality_gate_tools` não vazio |
| 3. File structure / source tree | PASS | Caminhos conferidos contra o código atual pós-merge da Story 7.2 (`metas/page.tsx` linha a linha, `src/app/(app)/page.tsx` linha a linha, `src/lib/agenda.ts`) |
| 4. AC coverage pelas tasks | PASS | 8 ACs, todas mapeadas em pelo menos uma task (Tasks 1–5) |
| 5. Testing guidance | PASS | Task 2 replica os casos já cobertos em `metas.test.ts`, agora na camada que monta a query, mais um caso novo de filtragem de período pra `metasAtivasHoje` |
| 6. Anti-alucinação | PASS | Toda claim técnica reverificada contra o código atual (pós-Story 7.2): `metas/page.tsx` (código exato do `Promise.all` a ser extraído, incluindo `SELECT_ATENDIMENTO`), `page.tsx` (linha exata onde `profissionais` é declarado e filtrado por `ativo: true`, confirmando o conflito de nome antes de propor a correção), `agenda.ts` (assinatura exata de `temConflito` como precedente de DI) |
| 7. CodeRabbit Integration | PASS | Completa desde a criação |

### Correção aplicada nesta validação (mecânica, sem alterar escopo)

Draft original mandava a Task 4 buscar `profissionais` em `/` sem notar que essa variável **já existe** na página, filtrada por `ativo: true` pra outro propósito (colunas da agenda). Reaproveitar esse nome/valor pra montar `regrasPorProfissional` faria uma meta ou comissão de profissional inativo ser silenciosamente ignorada na notificação — o mesmo tipo de bug de "reaproveitar dado filtrado pro propósito errado" que já apareceu em stories anteriores do projeto, mas desta vez pego antes da implementação. Corrigido: Task 4 agora nomeia explicitamente uma variável separada (`todosProfissionais`, sem filtro de `ativo`) pra esse fim.

### Observação da PO

A decisão de extrair `progressoDaMeta`/`metasAtivasHoje` como funções com `prisma` injetado (não importado direto), seguindo exatamente o precedente de `temConflito`, é a escolha certa — torna a extração testável sem `vi.mock` e é consistente com o único outro precedente de função server-side compartilhada que o projeto já tem. Nenhuma correção adicional necessária.

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** profissional ou dono do salão,
**I want** ver quando uma meta é batida e, se for a minha, quanto falta pra chegar lá,
**so that** eu saiba o resultado sem precisar abrir a tela de Metas toda hora.

## Acceptance Criteria

1. Ao carregar `/` (Agenda), se existir alguma meta (do salão ou de qualquer profissional) cujo período atual inclui a data de hoje e cujo progresso já atingiu 100%, uma notificação "Meta batida" aparece — visível a `DONO`, `BALCAO` e `PROFISSIONAL` igualmente.
2. Se a sessão for `PROFISSIONAL` e existir uma meta vinculada ao `profissionalId` dessa sessão com período incluindo hoje e progresso ainda abaixo de 100%, uma mensagem privada aparece: quanto falta (`valorAlvo - valorAtual`) pra bater aquela meta.
3. A mensagem de incentivo (AC 2) é estritamente privada — nunca aparece pra `DONO`/`BALCAO`, nem pra outro profissional que não seja o dono daquela meta.
4. Meta do salão nunca gera mensagem de incentivo individual (ela é sempre "de todos", nunca "de alguém") — só entra na notificação de "batida" do AC 1 quando atingida.
5. O cálculo de progresso usado nesta story é o mesmo da Story 7.2, extraído para uma função compartilhada (`src/lib/metas-progresso.ts`, com `prisma` injetado por parâmetro — mesmo padrão de `temConflito` em `src/lib/agenda.ts`) e reusado tanto por `/metas` (sem mudar nenhum resultado ali) quanto por `/`.
6. Sem persistência de "já visto": a notificação aparece toda vez que a condição for verdadeira e desaparece sozinha quando deixar de ser (meta sai do período, é excluída, ou é batida/deixa de estar em aberto) — sem tabela nova, sem estado de leitura.
7. Nenhuma mudança de comportamento em `/metas` (mesmos números exibidos, só reorganização interna do cálculo) nem no restante da Agenda além da nova seção de notificações no topo.
8. `npm run test`, `npm run lint` e `npm run build` continuam verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Architecture (extração de função compartilhada) + Frontend (notificações na Agenda) + Security (privacidade da mensagem individual)
**Secondary Type(s)**: —
**Complexity**: Média — a extração da Task 1 toca código já testado da Story 7.2 (`/metas`), então precisa preservar o comportamento exato; a lógica de "quem vê o quê" (AC 1 vs AC 2/3) é a parte sensível

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco em confirmar que a mensagem individual nunca vaza pra outro papel/profissional, e que `/metas` continua mostrando exatamente os mesmos números depois da extração)

**Supporting Agents**:
- — (sem mudança de schema nesta story)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando for pra PR
- [ ] Pre-Deployment: N/A — sem migração, sem variável de ambiente nova

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (mensagem de incentivo aparecendo pra sessão errada — vazamento de dado de outro profissional): auto_fix
- HIGH (`/metas` mudando de comportamento depois da extração da Task 1): auto_fix se persistir, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- A mensagem de incentivo (AC 2/3) só renderiza quando `sessao.papel === PAPEL_PROFISSIONAL && meta.profissionalId === sessao.profissionalId` — checado no componente/página, não só "escondido" no CSS
- `/metas/page.tsx` depois da extração produz exatamente os mesmos `valorAtual`/`percentual` de antes — comparar mentalmente com o código da Story 7.2 antes de aceitar como equivalente

**Secondary Focus**:
- Meta do salão (`profissionalId: null`) nunca passa pelo caminho de "mensagem individual", em nenhuma branch
- `metasAtivasHoje` usa o mesmo padrão de limite de período (`periodoInicio <= hoje < inicioDoDiaSeguinte(periodoFim)`) já estabelecido nas Stories 7.1/7.2

## Tasks / Subtasks

- [ ] **Task 1 — Extrair `progressoDaMeta`** (AC: 5, 7) em `src/lib/metas-progresso.ts` (novo arquivo)
  - [ ] `export async function progressoDaMeta(db: Pick<PrismaClient, "agendamento">, meta: { profissionalId: string | null; tipo: string; periodoInicio: Date; periodoFim: Date; valorAlvo: number }, regrasPorProfissional: Record<string, RegraComissao[]>, base: ComissaoBase, taxasPorForma: Partial<Record<FormaPagamento, ConfigTaxa>>): Promise<{ valorAtual: number; percentual: number }>` — corpo é exatamente o código hoje inline no `Promise.all` de `metas/page.tsx` (busca de atendimentos com `SELECT_ATENDIMENTO`, `comissaoPorProfissionalComServico`, `valorAtingido`, `percentualAtingido`), só movido pra cá com `db` (não `prisma` direto) como primeiro parâmetro — mesmo padrão de `temConflito` em `src/lib/agenda.ts`
  - [ ] `export async function metasAtivasHoje(db: Pick<PrismaClient, "meta">, hoje: Date): Promise<{ id: string; tipo: string; profissionalId: string | null; profissionalNome: string | null; periodoInicio: Date; periodoFim: Date; valorAlvo: number }[]>` — busca `meta.findMany({ where: { periodoInicio: { lte: hoje }, periodoFim: { gte: inicioDoDia(hoje) } }, include: { profissional: { select: { nome: true } } } })` (uma meta está "ativa hoje" quando `periodoInicio <= hoje` e `hoje` ainda cabe dentro do dia de `periodoFim` — comparar com `inicioDoDia(hoje)` no lado de `periodoFim`, não `hoje` bruto, pra não excluir o próprio dia de `periodoFim`)
- [ ] **Task 2 — Testes da extração** (AC: 5) em `src/lib/metas-progresso.test.ts` (novo arquivo)
  - [ ] `progressoDaMeta` com um `db` fake (objeto plano, sem `vi.mock` — mesmo espírito de dependency injection de `temConflito`, sem precisar mockar módulo nenhum): meta de profissional Faturamento, meta de profissional Comissão, meta do salão, profissional sem atendimento no período → replicam os mesmos casos já cobertos em `src/lib/metas.test.ts`, agora na camada que efetivamente monta a query
  - [ ] `metasAtivasHoje` com metas em períodos diferentes (uma ativa hoje, uma no passado, uma no futuro) → retorna só a ativa
- [ ] **Task 3 — Atualizar `/metas/page.tsx` pra usar a extração** (AC: 5, 7)
  - [ ] Trocar o corpo do `Promise.all(metasDb.map(...))` por `progressoDaMeta(prisma, m, regrasPorProfissional, base, taxasPorForma)` — sem mudar nenhum outro código da página (ranking intocado)
- [ ] **Task 4 — Notificações na Agenda** (AC: 1, 2, 3, 4) em `src/app/(app)/page.tsx`
  - [ ] **Atenção**: `page.tsx` já declara `profissionais` (linha 56–59 hoje) filtrado por `where: { ativo: true }` — usado só pra montar as colunas da agenda. Isso **não pode** ser reaproveitado pra montar `regrasPorProfissional`, porque um profissional inativo pode ter uma meta e uma `ComissaoRegra` que ainda precisam contar (mesmo raciocínio de "Inclui inativos" já usado em `comissoes/page.tsx`). A busca pra notificações usa uma variável própria, ex. `todosProfissionais = await prisma.profissional.findMany({ include: { comissaoRegras: true } })` (sem `where: { ativo: true }`), **separada** da `profissionais` já existente
  - [ ] Depois de resolver `sessao`/`ehProfissional` (já existe da Story 6.2) e **antes** do `return` cedo de "conta sem vínculo": buscar `todosProfissionais`/`config`/`taxasPorForma` (mesmo setup de `/comissoes`/`/metas`) + `metasAtivasHoje(prisma, new Date())`, rodar `progressoDaMeta` em cada uma (`Promise.all`)
  - [ ] `metasBatidas = metas.filter(m => m.percentual >= 100)` — vira uma lista de banners "Meta batida: {escopo} — {tipo}", visível sempre que não-vazia, independente do papel
  - [ ] `metaPropriaEmAberto = ehProfissional ? metas.find(m => m.profissionalId === sessao.profissionalId && m.percentual < 100) : undefined` — só quando `ehProfissional`; nunca lido/calculado pra `DONO`/`BALCAO`
  - [ ] Renderizar as duas seções (batidas + incentivo próprio, se houver) num bloco simples acima de `<AgendaControles>`, reaproveitando `.card`/`.badge` já existentes — sem CSS novo
- [ ] **Task 5 — Validar**: `npm run test && npm run lint && npm run build`

## Dev Notes

### Contexto (arquivos relevantes já existentes)

- `src/app/(app)/metas/page.tsx` (Story 7.2): hoje calcula `valorAtual`/`percentual` de cada meta inline, dentro de `Promise.all(metasDb.map(async (m) => {...}))` — usa `SELECT_ATENDIMENTO`, `comissaoPorProfissionalComServico`, `valorAtingido`, `percentualAtingido`. Este é o código que a Task 1 move, sem alterar, pra `metas-progresso.ts`. `[Source: src/app/(app)/metas/page.tsx]`
- `src/lib/agenda.ts`: `temConflito(db: Pick<PrismaClient, "agendamento">, {...})` — precedente exato do padrão de injeção de dependência que `progressoDaMeta`/`metasAtivasHoje` seguem: tipar o parâmetro como `Pick<PrismaClient, "...">` (só os models usados), não `PrismaClient` inteiro nem importar `@/lib/prisma` direto no arquivo — isso é o que torna a função testável sem `vi.mock`. `[Source: src/lib/agenda.ts]`
- `src/lib/metas.ts` (Story 7.2): `valorAtingido`/`percentualAtingido` continuam existindo exatamente como estão — `metas-progresso.ts` as importa e usa, não as substitui.
- `src/app/(app)/page.tsx` (Story 6.2): já resolve `sessao = await exigirSessao()` e `ehProfissional = sessao.papel === PAPEL_PROFISSIONAL` logo no início, com um `return` cedo pra "conta sem vínculo". A Task 4 insere a busca de notificações **depois** dessa checagem (não faz sentido buscar metas pra uma sessão sem `profissionalId` válido), mas **antes** do resto da lógica de agenda em si. `[Source: src/app/(app)/page.tsx]`
- `src/lib/datas.ts`: `inicioDoDia`, `inicioDoDiaSeguinte` — mesmos helpers já usados nas Stories 7.1/7.2 pra limites de período.

### Por que "ativa hoje" compara com `inicioDoDia(hoje)` no lado de `periodoFim`

`periodoFim` é gravado como meia-noite do último dia do período (nota já registrada na Story 7.1). Comparar `periodoFim >= hoje` com `hoje` na hora exata do request (ex.: 14h32) excluiria incorretamente o próprio último dia do período, já que meia-noite é sempre "menor" que qualquer hora depois dela no mesmo dia. Normalizando `hoje` pra `inicioDoDia(hoje)` antes de comparar, o último dia do período conta como ativo o dia inteiro — mesmo raciocínio do "limite superior exclusivo" já usado nas queries de atendimento, só que aqui os dois lados da comparação precisam estar na mesma granularidade (meia-noite a meia-noite), não um exclusivo e outro inclusivo.

### Por que a extração (Task 1) não muda nenhuma linha de lógica, só o local

O objetivo desta story não é melhorar o cálculo de progresso — é reusá-lo em um lugar novo. Qualquer mudança de comportamento em `progressoDaMeta` durante a extração seria uma regressão silenciosa na Story 7.2 (já em produção). Por isso a Task 1 é explícita: mover o código, não reescrevê-lo.

### Testing

- **Unit:** ver Task 2 — `progressoDaMeta`/`metasAtivasHoje` testadas com objetos `db` fake simples (sem `vi.mock`, já que a injeção de dependência elimina a necessidade). `src/lib/metas.test.ts` (Story 7.2) continua intacto e cobrindo `valorAtingido`/`percentualAtingido` isoladamente.
- **Manual:** criar uma meta de profissional com valor-alvo bem baixo (pra já nascer "batida" com dados existentes) e período incluindo hoje; logar como esse profissional em `/` → ver o banner de "batida". Criar uma segunda meta do mesmo profissional com valor-alvo alto (não batida) e período incluindo hoje; conferir que aparece "faltam R$X" só pra esse profissional, e que logando como outro profissional ou como `BALCAO` essa mensagem não aparece (só a de "batida", se houver alguma).

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-17 | 0.1     | Draft inicial — terceira e última story do Epic 7, conforme PRD | River (@sm) |
| 2026-09-17 | 1.0     | Validada GO (9/10) — corrigido conflito de nome com `profissionais` já filtrado por ativo em `page.tsx` — Status: Draft → Ready | Pax (@po) |

## Dev Agent Record

_A preencher pelo @dev durante a implementação._

## QA Results

_A preencher pelo @qa após a implementação._
