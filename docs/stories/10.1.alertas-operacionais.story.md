# Story 10.1: Resumo diário de estoque baixo + caixa não fechado

## Status

Ready

## PO Validation (2026-09-24 — Pax, revisão abreviada)

**Verdict: GO.** Sem migração. Independente das Stories 9.1/11.1 — arquivos próprios
(`src/app/api/alertas-operacionais/`), e o suporte a `templateName` em `enviarWhatsapp` já foi
adiantado numa mudança preparatória separada, então não há edição concorrente do mesmo arquivo.

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** receber um resumo diário se algum produto está acabando ou se esqueci de fechar o caixa,
**so that** eu não precise abrir o app todo dia só pra checar isso.

## Acceptance Criteria

1. Nova rota `GET /api/alertas-operacionais` (fora do grupo `(app)`, sem sessão — protegida por
   `Authorization: Bearer <ALERTAS_CRON_SECRET>`, mesmo contrato de `/api/lembretes`/`/api/retencao`).
2. A rota verifica: (a) produtos ativos em alerta de estoque, reusando a função pura `emAlerta` já
   existente (`src/lib/estoque.ts`); (b) se existe `FechamentoCaixa` para o dia anterior à execução —
   ausência de registro = "caixa não fechado".
3. Quando houver pelo menos um item a reportar (produtos em alerta **ou** caixa de ontem não fechado),
   um resumo é enviado ao dono por e-mail e/ou WhatsApp, reusando `enviarEmail`/`enviarWhatsapp` do
   Epic 8. **Sem nada a reportar, nenhuma mensagem é enviada** — nem vazia, nem "está tudo bem".
4. O destinatário é lido de `DONO_EMAIL`/`DONO_TELEFONE` (variáveis de ambiente) — sem cadastro na UI.
   Sem nenhuma das duas configuradas, a rota funciona normalmente e simplesmente não envia nada
   (mesma degradação graciosa dos outros canais), mas ainda responde 200 com a contagem apurada.
5. Sem `ALERTAS_CRON_SECRET` configurado, a rota responde **503**; header ausente/errado responde
   **401** — mesmo contrato das rotas de cron já existentes.
6. `.env.example`/`DEPLOY.md` documentam `ALERTAS_CRON_SECRET`, `DONO_EMAIL`, `DONO_TELEFONE`, e o
   Cron Job (sugestão: 1x/dia, de manhã) apontando pra `/api/alertas-operacionais`.
7. `npm run test`, `npm run lint` e `npm run build` continuam verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: API (nova rota)
**Secondary Type(s)**: Integration (reusa e-mail/WhatsApp já existentes)
**Complexity**: Baixa — terceira rota no mesmo padrão já validado (`/api/lembretes`, `/api/retencao`); a
única lógica nova é montar o texto do resumo a partir de duas consultas simples

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @qa (quality gate — foco em: nenhuma mensagem enviada quando não há nada a reportar; produto
  inativo nunca aparece no resumo)

**Supporting Agents**:
- @architect (referência: terceira rota no mesmo padrão de `/api/lembretes`/`/api/retencao`)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando for pra PR
- [ ] Pre-Deployment: sem migração; passo manual do Cron Job documentado no `DEPLOY.md`

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (mensagem enviada mesmo sem nada a reportar, ou produto inativo aparecendo no alerta):
  auto_fix
- HIGH (`emAlerta` reimplementada inline em vez de importada, duplicando a lógica de negócio): auto_fix

### CodeRabbit Focus Areas

**Primary Focus**:
- `emAlerta` importada de `src/lib/estoque.ts`, nunca reimplementada
- Nenhuma mensagem (nem "tudo certo") é enviada quando não há nada a reportar

**Secondary Focus**:
- `FechamentoCaixa` do dia anterior consultado pela mesma convenção de data (`inicioDoDia`) já usada em
  `src/app/(app)/caixa/actions.ts`

## Tasks / Subtasks

- [ ] **Task 1 — Rota de alertas** (AC: 1, 2, 3, 4, 5) em `src/app/api/alertas-operacionais/route.ts` (novo, mesma estrutura das outras rotas de cron)
  - [ ] Mesmo padrão de 503/401 de `/api/lembretes`/`/api/retencao`, trocando pra `ALERTAS_CRON_SECRET`
  - [ ] `const produtos = await prisma.produto.findMany(); const emAlertaLista = produtos.filter(emAlerta);` (import de `@/lib/estoque`)
  - [ ] `const ontem = new Date(Date.now() - 24 * 60 * 60000); const fechamentoOntem = await prisma.fechamentoCaixa.findUnique({ where: { data: inicioDoDia(ontem) } });` (import `inicioDoDia` de `@/lib/datas`) — `caixaNaoFechado = fechamentoOntem === null`
  - [ ] Se `emAlertaLista.length === 0 && !caixaNaoFechado`, retornar `{ enviado: false, produtosEmAlerta: 0, caixaNaoFechado: false }` **sem** chamar `enviarEmail`/`enviarWhatsapp`
  - [ ] Senão, montar o texto do resumo (lista de nomes dos produtos em alerta + aviso de caixa não fechado, quando aplicável) e chamar `enviarEmail`/`enviarWhatsapp` pro `DONO_EMAIL`/`DONO_TELEFONE` (cada canal só se a respectiva env var estiver setada — `DONO_TELEFONE` passa por `normalizarTelefoneBR` antes, mesmo padrão das outras rotas); retornar `{ enviado: true, produtosEmAlerta: emAlertaLista.length, caixaNaoFechado }`
  - [ ] `src/middleware.ts`: adicionar `api/alertas-operacionais` à exceção do matcher
- [ ] **Task 2 — Templates de mensagem** (AC: 3) em `src/lib/email.ts`
  - [ ] `emailAlertasOperacionais({ produtos, caixaNaoFechado }: { produtos: string[]; caixaNaoFechado: boolean }): { subject: string; html: string }` — mesmo padrão de `base()`/`escapeHtml` já existente; lista os nomes dos produtos (escapados) e menciona o caixa não fechado quando `true`
  - [ ] WhatsApp: reusar `enviarWhatsapp` com `templateName: process.env.WHATSAPP_TEMPLATE_ALERTAS_NAME` (mesmo mecanismo de `templateName` opcional adicionado na Story 9.1) e `parametros` reduzidos a um resumo textual simples (ex.: `["3 produtos em alerta, caixa de ontem não fechado"]` — 1 variável, mesmo espírito do template de retenção)
- [ ] **Task 3 — Documentação** (AC: 6)
  - [ ] `.env.example`: `ALERTAS_CRON_SECRET=`, `DONO_EMAIL=`, `DONO_TELEFONE=`, `WHATSAPP_TEMPLATE_ALERTAS_NAME=`
  - [ ] `DEPLOY.md`: variáveis na tabela existente + Cron Job de `/api/alertas-operacionais` (sugestão 1x/dia de manhã) + nota do terceiro template do WhatsApp (1 variável, resumo textual)
- [ ] **Task 4 — Testes** (AC: 2, 3, 4, 5) em `src/app/api/alertas-operacionais/route.test.ts` (novo)
  - [ ] Sem produto em alerta e caixa de ontem fechado → `enviado: false`, nenhum canal chamado
  - [ ] Produto em alerta (`ativo: true`, `estoqueAtual < estoqueMinimo`) → `enviado: true`, `enviarEmail`/`enviarWhatsapp` chamados
  - [ ] Produto abaixo do mínimo mas **inativo** → não conta como alerta (reusa a mesma regra de `emAlerta`, testada isoladamente na Story 3.3 — aqui só confirmar que a rota não reimplementa a condição)
  - [ ] Caixa de ontem não fechado (nenhum `FechamentoCaixa` pra aquela data) mesmo sem produto em alerta → `enviado: true`
  - [ ] Sem `DONO_EMAIL`/`DONO_TELEFONE` configurados, mesmo com algo a reportar → não lança erro, `enviado` reflete se pelo menos um canal teria sido tentado (ambos vazios → nenhuma chamada de canal, mas resposta ainda é 200)
  - [ ] Sem `ALERTAS_CRON_SECRET` → 503; header errado → 401
- [ ] **Task 5 — Validar**: `npm run test && npm run lint && npm run build`

## Dev Notes

### Contexto (arquivos relevantes já existentes)

- `src/lib/estoque.ts`: `emAlerta({ ativo, estoqueAtual, estoqueMinimo }): boolean` — função pura já
  testada na Story 3.3, única fonte de verdade da regra de alerta de estoque. `[Source:
  src/lib/estoque.ts]`
- `src/app/(app)/caixa/actions.ts:23-27`: `janelaDoDia`/`inicioDoDia` — `FechamentoCaixa.data` é gravado
  como `inicioDoDia(dia)` (meia-noite local); a consulta do dia anterior nesta story precisa usar a
  mesma convenção (`inicioDoDia(ontem)`), senão o `findUnique` nunca bate com o registro existente.
  `[Source: src/app/(app)/caixa/actions.ts]`
- `src/app/api/lembretes/route.ts` / `src/app/api/retencao/route.ts`: padrão de rota de cron (503/401,
  canais condicionados à própria disponibilidade) a replicar aqui pela terceira vez.
- `src/lib/email.ts` / `src/lib/whatsapp.ts`: `enviarEmail`, `enviarWhatsapp` (com `templateName`
  opcional, adicionado na Story 9.1) — reusados sem mudança de assinatura.

### Testing

- **Unit:** ver Task 4 — mesmo padrão de mock de `@/lib/prisma`, `@/lib/email`, `@/lib/whatsapp` já
  usado nas rotas de cron anteriores.
- **Manual:** com um produto de teste abaixo do mínimo e o caixa de ontem não fechado, chamar
  `curl -H "Authorization: Bearer <secret>" http://localhost:3000/api/alertas-operacionais` e conferir
  o recebimento do resumo.

## Change Log

| Date       | Version | Description            | Author       |
|------------|---------|------------------------|--------------|
| 2026-09-24 | 0.1     | Draft inicial — primeira story do Epic 10, conforme PRD | River (@sm) |
| 2026-09-24 | 1.0     | Validada GO (revisão abreviada) — Status: Draft → Ready | Pax (@po) |
