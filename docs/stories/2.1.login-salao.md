# Story 2.1: Login do salão

## Status

Ready

## PO Validation (2026-09-10 — Pax)

**Verdict: GO** — Clarity 8/10. Pronta para implementação. Sem dependências de outra story; desbloqueia 2.2 (papéis) e é pré-requisito para divulgar a URL pública.

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Valor (dados abertos na URL pública) e mudança de premissa (NFR4→NFR10) explícitos |
| 2. Technical Implementation Guidance | PASS | Lib de sessão, hash, ponto de proteção, route group, entidade `Usuario` com `papel` — tudo apontado; decisões abertas listadas com recomendação |
| 3. Reference Effectiveness | PASS | Referencia PRD FR15–17 / NFR10–13, `docs/architecture.md#1.5` (premissa revogada), e os padrões de server action do Epic 1 |
| 4. Self-Containment | PASS | Fallback do middleware (Edge) documentado; edge cases de `SESSION_SECRET` ausente e cookie adulterado cobertos |
| 5. Testing Guidance | PASS | Casos da action de login (mesma mensagem nos 3 cenários) + roteiro manual de 6 passos |
| 6. CodeRabbit Integration | PASS | Foco em segredo commitado, flags de cookie, cobertura de rota, enumeração de usuário |

**Não bloqueante (para o @dev observar):**
- **AC7 (timing):** "sem diferença de tempo perceptível" é aspiracional; a mitigação concreta (comparar sempre contra um hash dummy quando o usuário não existe) está na Task 4 — implementar isso satisfaz a AC. Não exigir constant-time perfeito.
- **`.env.example`:** o arquivo atual é o do framework AIOX e **não tem `DATABASE_URL`**. Ao atualizar (Task 8), acrescentar também `DATABASE_URL` além das 4 variáveis novas, para o exemplo ficar utilizável de fato.
- **Arquitetura Epic 2:** não há seção de arquitetura para o Epic 2 ainda. O quality gate é `@architect` — que deve confirmar o padrão de sessão / runtime do middleware na revisão e, idealmente, registrar a decisão em `docs/architecture.md`.
- **Seed do admin em produção:** garantir no `DEPLOY.md` que o passo `npm run db:seed:admin` roda **depois** do `prisma migrate deploy` (tabela `Usuario` precisa existir) e **uma vez só**.

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [npm run test, npm run lint, npm run build]

## Story

**As a** dono do salão,
**I want** proteger o acesso ao sistema com um login,
**so that** os dados do salão (clientes, telefones, faturamento, comissões) não fiquem abertos a quem tiver o link.

## Acceptance Criteria

1. Existe a rota `/login` com formulário (usuário + senha), acessível sem sessão; é a única rota (além de assets estáticos) fora da área protegida nesta story.
2. Qualquer outra rota, acessada sem sessão válida, redireciona para `/login`. A checagem é feita em um único ponto (middleware ou guarda em layout de servidor), não repetida em cada página.
3. A credencial do "dono" é criada por um comando de seed que grava um registro `Usuario` com a senha em **hash** (bcrypt). O usuário e a senha inicial vêm de variáveis de ambiente (`ADMIN_USER`, `ADMIN_PASSWORD`); nunca há senha em texto puro no repositório nem no banco.
4. A sessão é mantida por **cookie HTTP-only, assinado/criptografado**; a chave vem da variável de ambiente `SESSION_SECRET` (mín. 32 caracteres). O app não sobe sem `SESSION_SECRET` definido.
5. Há ação **"Sair"** visível no layout (na barra lateral) que encerra a sessão e leva a `/login`.
6. A sessão expira após período de inatividade configurável, default **12 horas** (`SESSION_TTL_HOURS`).
7. Login inválido retorna mensagem **genérica** em pt-BR ("Usuário ou senha inválidos.") — sem revelar qual campo falhou e sem diferença de tempo perceptível entre "usuário não existe" e "senha errada".
8. `npm run build`, `npm run lint`, o typecheck e `npm run test` passam. A migração que cria `Usuario` é não destrutiva (tabela nova; nenhum dado existente afetado).
9. Após login bem-sucedido, o usuário é levado para `/` (agenda). Acessar `/login` já autenticado redireciona para `/`.
10. `DEPLOY.md` e `.env.example` são atualizados com as novas variáveis (`ADMIN_USER`, `ADMIN_PASSWORD`, `SESSION_SECRET`, `SESSION_TTL_HOURS`) e o passo de rodar o seed do usuário uma vez em produção.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Segurança / Auth (middleware + server action + sessão)
**Secondary Type(s)**: Database (entidade `Usuario`), Frontend (tela de login)
**Complexity**: Média (fluxo de sessão, runtime do middleware, hashing)

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @architect (quality gate — confirmar padrão de sessão e runtime do middleware)

**Supporting Agents**:
- @qa (revisão de segurança — enumeração de usuário, flags de cookie, expiração)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `npm run test`, `npm run lint`, `npm run build`
- [ ] Pre-PR (@devops): quando o Epic 2 for para PR
- [ ] Pre-Deployment: garantir `SESSION_SECRET` e credenciais setadas no Railway antes do deploy

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL (segredo vazado, cookie sem HttpOnly, rota desprotegida): auto_fix
- HIGH: document_as_debt se persistir

### CodeRabbit Focus Areas

**Primary Focus**:
- Nenhum segredo (senha, `SESSION_SECRET`) commitado; `.env` fora do git (já está)
- Cookie de sessão com `httpOnly`, `sameSite: "lax"`, `secure` em produção
- Cobertura de rota: nenhuma página protegida acessível sem sessão (inclusive rotas novas do Epic 2)
- Mensagem de erro genérica; sem enumeração de usuário (timing e texto)

**Secondary Focus**:
- Hash com custo adequado (bcrypt cost ≥ 10)
- Middleware não faz operação de Node incompatível com o runtime Edge (só lê/valida o cookie)
- App falha rápido e claro se `SESSION_SECRET` estiver ausente

## Tasks / Subtasks

- [ ] **Task 1 — Entidade `Usuario`** (AC: 3, 8)
  - [ ] `schema.prisma`: `model Usuario { id String @id @default(cuid()); usuario String @unique; senhaHash String; papel String @default("DONO"); criadoEm DateTime @default(now()) }`
  - [ ] `npx prisma migrate dev --name epic2_usuario` — conferir SQL: só `CREATE TABLE`
  - [ ] Campo `papel` já entra aqui (default `"DONO"`) para a Story 2.2 não precisar de outra migração; nesta story ele não é usado para autorizar nada
- [ ] **Task 2 — Seed do usuário admin** (AC: 3, 10)
  - [ ] `prisma/seed-admin.ts`: lê `ADMIN_USER` e `ADMIN_PASSWORD` do ambiente; se faltar, erro claro e `exit(1)`
  - [ ] `upsert` do `Usuario` por `usuario` com `senhaHash = bcrypt.hash(ADMIN_PASSWORD, 10)`
  - [ ] `package.json`: script `"db:seed:admin": "tsx prisma/seed-admin.ts"`
  - [ ] Não incluir no `prisma/seed.ts` principal (que é dados de exemplo e roda com `--force`)
- [ ] **Task 3 — Camada de sessão** (AC: 4, 6)
  - [ ] `src/lib/sessao.ts`: helpers `criarSessao(usuarioId)`, `lerSessao()`, `destruirSessao()` sobre cookie assinado
  - [ ] Usar `iron-session` (v8, compatível com App Router + middleware) OU cookie HMAC próprio com `jose`; decisão em Dev Notes
  - [ ] Ler `SESSION_SECRET` e `SESSION_TTL_HOURS` de `process.env`; lançar erro no boot se `SESSION_SECRET` ausente/curto
  - [ ] Cookie: `httpOnly: true`, `sameSite: "lax"`, `secure: process.env.NODE_ENV === "production"`, `maxAge` = TTL
- [ ] **Task 4 — Server action de login/logout** (AC: 7, 9)
  - [ ] `src/app/login/actions.ts`: `entrar(_prev, formData)` → busca `Usuario` por `usuario`, `bcrypt.compare`, sempre compara contra um hash (dummy se usuário não existe) para não vazar timing; sucesso → `criarSessao` + `redirect("/")`
  - [ ] `sair()` → `destruirSessao` + `redirect("/login")`
  - [ ] Retorno `{ ok: boolean; erro?: string }`; erro sempre `"Usuário ou senha inválidos."`
- [ ] **Task 5 — Tela `/login`** (AC: 1, 9)
  - [ ] `src/app/login/page.tsx` — Server Component; se já houver sessão, `redirect("/")`
  - [ ] Client component com `useActionState(entrar)`; layout simples fora do shell (sidebar não aparece no login)
- [ ] **Task 6 — Proteção das rotas** (AC: 2)
  - [ ] `src/middleware.ts`: `matcher` cobre tudo exceto `/login`, `/_next`, assets, favicon; sem sessão → `NextResponse.redirect("/login")`
  - [ ] Middleware só valida o cookie (compatível com Edge); a verificação de senha fica na server action (Node)
  - [ ] Se `iron-session` não rodar no Edge no projeto, cair para guarda no `src/app/layout.tsx` (Server Component lê a sessão e redireciona) — decidir e documentar
- [ ] **Task 7 — "Sair" no layout** (AC: 5)
  - [ ] `src/components/Nav.tsx` (ou o layout): botão/form que chama `sair()`; mostra o nome do usuário logado
- [ ] **Task 8 — Config e docs** (AC: 10)
  - [ ] `.env.example`: adicionar `ADMIN_USER`, `ADMIN_PASSWORD`, `SESSION_SECRET`, `SESSION_TTL_HOURS`
  - [ ] `DEPLOY.md`: seção "Autenticação" — setar as 3 variáveis no Railway + rodar `npm run db:seed:admin` uma vez (via Railway shell ou um deploy hook)
  - [ ] `railway.json`: manter `prisma migrate deploy` no start (já roda a migração nova); o seed do admin é passo manual único
- [ ] **Task 9 — Testes** (AC: 8)
  - [ ] `src/lib/sessao.test.ts` (se houver lógica pura de TTL/parse) OU teste da action de login com um `Usuario` fake: senha certa → ok; senha errada → `{ ok: false, erro: "Usuário ou senha inválidos." }`; usuário inexistente → mesmo erro
  - [ ] Evitar depender de `bcrypt` real no teste se ficar lento — usar cost baixo ou mockar o compare

## Dev Notes

### Contexto (código atual)

- Stack: Next.js 15 App Router, server actions, Prisma + SQLite, CSS puro. Sem nenhuma auth hoje
  (arquitetura §1.5 "Sem auth" — **esta story revoga essa premissa**, ver PRD NFR10).
- `src/app/layout.tsx` renderiza o shell (`<aside className="sidebar">` + `<Nav />` + `<main>`).
  A tela de login deve ficar **fora** desse shell — opções: (a) `layout.tsx` condiciona o shell
  pela presença de sessão; (b) route group `(app)` com o shell e `/login` fora dele. Recomendo (b).
- `src/lib/` já tem `prisma.ts`, `financeiro.ts`, `datas.ts`, `agenda.ts`, `format.ts`.
- Padrão de server action: `{ ok, erro }` + `useActionState` no client (ver `NovoAgendamento.tsx`,
  `AgendaAcoes.tsx`).
- `.env` já está no `.gitignore`; `.env.example` **não existe ainda** — criar.

### Decisões a tomar (registrar a escolha no Change Log)

1. **Biblioteca de sessão.** Recomendação: `iron-session` v8 — cookie criptografado, API simples,
   funciona em server actions e (v8) em middleware. Alternativa sem dep nova: assinar um JSON com
   `jose` (JWT HS256). `iron-session` é menos código e menos chance de erro. **Peso:** +1 dep pequena.
2. **Hash de senha.** `bcryptjs` (JS puro, sem build nativo — importante no Railway e no Windows).
   Cost 10. Evitar `bcrypt` nativo e `argon2` nativo aqui.
3. **Ponto de proteção.** Preferir `src/middleware.ts` (redireciona antes de renderizar). Se o
   runtime Edge do middleware conflitar com a leitura do cookie da lib escolhida, usar guarda no
   layout do route group `(app)`. Testar as duas rotas: `/` e uma rota de API/ação.
4. **Credencial: env + seed vs. só env.** Escolhido: **entidade `Usuario` + seed** (AC 3), porque a
   Story 2.2 precisa de `Usuario.papel` de qualquer forma. Fazer a migração agora evita retrabalho.

### Assunções explícitas

- Um único usuário (o dono) nesta story. Cadastro de mais usuários / troca de senha pela UI = Story
  2.2 ou posterior. Trocar a senha agora = rodar `db:seed:admin` de novo com `ADMIN_PASSWORD` novo.
- Sem "lembrar-me", sem recuperação de senha por e-mail, sem 2FA no Epic 2.
- Sem rate limiting / bloqueio por tentativas nesta story (salão pequeno, superfície pequena) —
  registrar como possível débito se a URL for muito exposta.
- O agendamento online público (fase 3) terá sua própria rota fora da proteção; não é problema desta
  story, mas o `matcher` do middleware deve ser fácil de estender.

### Edge cases

- `SESSION_SECRET` ausente → app não sobe, erro claro no log (não "funcionar sem proteger").
- Cookie adulterado/expirado → tratado como sem sessão → redirect para `/login`.
- Usuário acessa `/login` já logado → redirect para `/`.
- Deep link para `/caixa?data=...` sem sessão → após login, pode ir direto para `/` (não é
  obrigatório preservar o destino nesta story; se for trivial com o middleware, preservar via
  `?next=`).

### Relevant Source Tree

- `src/app/layout.tsx` — shell; será dividido em route group `(app)` + `/login` fora
- `src/components/Nav.tsx` — ganha "Sair" + nome do usuário
- `src/lib/prisma.ts` — reusar
- `prisma/schema.prisma` — nova model `Usuario`
- `prisma/seed.ts` — **não** mexer; criar `prisma/seed-admin.ts` separado
- `railway.json`, `DEPLOY.md`, `.env.example` (novo)

### Testing

- **Framework:** Vitest (já configurado). Focar na action de login (senha certa/errada/usuário
  inexistente → mesma mensagem) e em qualquer lógica pura de sessão (TTL).
- **Sem E2E** (fora de escopo, igual Epic 1).
- **Manual:**
  1. `ADMIN_USER=dono ADMIN_PASSWORD=troque123 npm run db:seed:admin` grava o usuário.
  2. Acessar `/` sem cookie → cai em `/login`.
  3. Login com senha errada → "Usuário ou senha inválidos.", permanece em `/login`.
  4. Login correto → vai para `/`, sidebar aparece, mostra "dono".
  5. "Sair" → volta para `/login`; `/` volta a redirecionar.
  6. Remover `SESSION_SECRET` do `.env` → `npm run build`/`dev` falha com mensagem clara.

## Change Log

| Date       | Version | Description                | Author       |
|------------|---------|----------------------------|--------------|
| 2026-09-10 | 0.1     | Draft inicial da story     | River (@sm)  |
| 2026-09-10 | 1.0     | Validada — GO, Draft → Ready | Pax (@po)  |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
